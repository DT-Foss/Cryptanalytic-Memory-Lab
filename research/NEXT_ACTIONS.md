# Ranked Next Actions

Last ranked: 2026-07-15T17:55:31+02:00.

| Rank | Action | SOTA potential | Information gain | Reuse | Cost | Decision unlocked |
|---:|---|---:|---:|---:|---:|---|
| 1 | `O1C-0008`: precommit multiple OS-random targets and generate matched solver trajectories in which every tested key bit is an explicit `0`/`1` assumption under equal budgets | State of the art | Very high | Very high | Medium | Creates the first genuinely fresh upstream-evidence test without relying on A355/A356 outcomes |
| 2 | Apply the exact O1C-0007 12-register decoder eventwise, persist every state and complete output before reveal, then evaluate matched numeric, public-hash, orientation-swap and random-label controls | State of the art | Very high | Very high | Medium | Determines whether the compact mechanism transfers rather than merely fitting a retrospective panel |
| 3 | Require reproducibility across unseen targets and a material downstream time-to-hit/search-space advantage; report exact joint/null evidence rather than one best rank | State of the art | Very high | High | Medium | Separates a real cryptanalytic memory advantage from chance and multiplicity |
| 4 | If unary evidence fails, localize signed deltas by carry, round, propagation, conflict and assumption-pair provenance before adding low-degree interactions or slots | State of the art | Very high | Very high | Medium | Converts a negative into a precise observability map and the next mechanism proposal |
| 5 | Attempt bounded full-round recovery and independent confirmation only after the fresh multi-target efficacy gate passes | State of the art | Very high | High | High | Converts fresh mechanistic evidence into a terminal frontier result |

## Hard gate before recovery or SOTA claim

The primary mechanism already satisfies the structural state gate. O1C-0008 must
now satisfy all of the following scientific-efficacy gates:

- retain the frozen 12-register/266-byte maximum logical mechanism state unless a
  separately registered successor is tested;
- no candidate-indexed table, cache, KV store or invertible 4,096-degree surrogate;
- bounded state in stream length and no full `O(T)` attention;
- no target labels or recovery feedback before order freeze;
- source-unseen disjoint targets, exact work accounting and complete persisted
  states/orders;
- matched numeric, public-hash, orientation-swap and exact/random-label controls;
- reproducible material downstream advantage, not a single favorable rank.

## Do not spend the next cycle on

- another broad O1/O1-O repository review;
- resweeping A355 or choosing a new O1C-0007 panel member after seeing its target;
- treating the frozen A356 order as a source-unseen test or reading its outcome to
  tune O1C-0008;
- another precision or headroom sweep of the 4,096-mode full-basis bank;
- more raw single-channel selection on the two A296 validation labels;
- more top-energy cutoffs on A348/A355/A356 without a new mechanism;
- treating 8,014-byte full-basis reconstruction as compression or SOTA;
- modifying the O1C-0007 decoder after fresh target generation without reserving a
  distinct successor attempt;
- reading sibling progress/outcome files or modifying any sibling repository;
- expensive full-domain recovery before the compact evidence gate passes;
- post-result ranks or recovered keys as discovery features.
