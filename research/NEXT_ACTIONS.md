# Ranked Next Actions

Last ranked: 2026-07-15T15:50:05+02:00.

| Rank | Action | SOTA potential | Information gain | Reuse | Cost | Decision unlocked |
|---:|---|---:|---:|---:|---:|---|
| 1 | `O1C-0007`: reproduce and capsule the sparse/common-mask/per-slot compact-support ladder under the corrected codec, with the 3,918-byte direct table as a hard eligibility gate | Very high | Very high | Very high | Low | Establishes exactly which final-field compression families are exhausted and preserves their breadcrumbs |
| 2 | Extract attacker-computable bit-, carry-, round- and solver-conflict evidence before it collapses into the pseudorandom final scalar score | State of the art | Very high | Very high | Medium | Tests the central O1 idea: accumulate reusable evidence about latent key bits rather than candidate rows |
| 3 | Implement a bounded bit/interaction memory with no 4,096-entry state, maximum serialized logical state below 3,918 bytes and disjoint transfer gates | State of the art | Very high | Very high | Medium | Creates the first mechanism eligible to challenge the direct-table baseline |
| 4 | Require the compact mechanism to beat matched numeric/public-hash/direct-table controls on both fidelity and downstream time-to-hit before freezing it | State of the art | Very high | High | Medium | Separates a real memory advantage from attractive representation changes |
| 5 | Only after ranks 1–4 pass, bind one OS-random W46 challenge, persist every complete order before recovery and independently confirm any full-round candidate | State of the art | Very high | High | High | Converts the mechanism into fresh full-round recovery evidence |

## Hard gate before a fresh challenge

The primary mechanism must satisfy all of the following:

- fewer than 3,918 bytes of maximum serialized logical mechanism state;
- no candidate-indexed table, cache, KV store or invertible 4,096-degree surrogate;
- bounded state in stream length and no full `O(T)` attention;
- no target labels or recovery feedback before order freeze;
- disjoint transfer evidence, exact work accounting and complete persisted orders;
- a material downstream advantage, not score-field correlation alone.

## Do not spend the next cycle on

- another broad O1/O1-O repository review;
- another precision or headroom sweep of the 4,096-mode full-basis bank;
- more raw single-channel selection on the two A296 validation labels;
- more top-energy cutoffs on A348/A355/A356 without a new mechanism;
- treating 8,014-byte full-basis reconstruction as compression or SOTA;
- generating the fresh challenge before a compact eligible mechanism exists;
- reading sibling progress/outcome files or modifying any sibling repository;
- expensive full-domain recovery before the compact evidence gate passes;
- post-result ranks or recovered keys as discovery features.
