# O1C-0023 — Deterministic Post-Result Native Composer

- Recorded: `2026-07-18T02:57:53+02:00`
- Status: source-frozen instrument; prerequisite pending; no attempt reserved
- Claim level: `RETROSPECTIVE`
- Upstream science: future authoritative finalized `O1C-0022` only
- Fresh targets, solver branches, entropy, MPS and GPU calls: `0`

## Purpose

O1C-0023 converts the complete finalized O1C-0022 result into exactly one
hash-bound successor operator. It is the first deterministic O1-O composition
step in the living-inverse program: completed evidence selects a mechanism,
failure memory prevents an identical failed context from repeating, and native
O1-O routes the opaque decision into a data-only operator fragment. It does not
run the selected successor and never treats generated source as scientific
evidence.

The scientific authority remains the lab-owned deterministic policy. O1-O is the
native router and assembler, not a scorer, verifier or target broker. Every
decision keeps `fresh_target_authorized=false`; even an O1C-0022 scientific pass
only proposes the separately source-frozen prospective attempt that may later
request one untouched target.

## Exact upstream boundary

The runner accepts only the canonical `RunCapsuleManager` publication reserved as
`O1C-0022`. A caller-created `FinalizedRun`, a structurally small fake capsule or
a merely self-consistent result is rejected. Before O1C-0023 can reserve itself,
the runner reopens and binds:

- the exact O1C-0022 config and its `ce56ba4` source ancestry;
- all 38 pinned source hashes;
- all 384 indexed artifacts, their bytes, phases and full tree inventory;
- four folds of exactly 36 calibration and 58 held-out artifacts;
- the eight post-freeze scored artifacts and their fixed byte shapes;
- all calibration and prediction freeze commitments;
- four held-out K256 execution ledgers, 352-byte primary states and execution
  receipts;
- the scored result, outer capsule metrics, recomputed classification, gates,
  margins, resources and operational budget outcome.

The exact upstream work surface is 32 reader replays, 17,664 packet slots,
1,130,496 public work units and 7,391,232 calibration-value evaluations. Every
declared resource gate is recomputed rather than trusted.

## Decision ladder

The policy consumes the full result/metrics surface plus K256 quantization facts,
not the classification label alone. The main mechanism ladder is:

1. replay only an operationally failed publication/resource surface;
2. repair the first failed integrity or lifecycle invariant;
3. localize width collapse with deterministic coordinate-bank resets;
4. separate scale weighting, int8-only denoising, quantizer precision/clip and a
   true multi-slot residual-vault condition;
5. activate proof-ancestry pairs or exact contradiction antecedents only when all
   real float arms are null;
6. repair coordinate binding, horizon compounding or magnitude confidence when
   the corresponding matched control names that failure;
7. stabilize fold/width growth when real signal exists but the full pass is not
   robust;
8. propose one separately frozen full-256 prospective lineage only after every
   O1C-0022 scientific and integrity gate passes.

The all-float-null branch is the only branch allowed to use the A539/A541
breadcrumb. It preserves proof identity but explicitly excludes the closed
additive single-position marginal. K256 saturation is not inferred from the
existence of the `|q|=8` rounding-limit bin: the composer distinguishes zero
slots, rounding-limit occupancy and actual pre-clip magnitude at `>=8.5`.

## Failure memory

Failure memory is immutable canonical input, not mutable global state. Each entry
binds a finalized capsule manifest, decision hash, operator fingerprint, context
fingerprint and outcome. Scientific no-lift closes only that exact operator in
that exact evidence context. Operational launch/publication failure remains a
replay breadcrumb and does not falsely close the mechanism. If every currently
legal operator for one context is closed, the policy emits an explicit novel
policy-extension requirement instead of cycling.

## Native O1-O assembly

Each of two independent native invocations receives only:

- one opaque decision-token triplet in `bridge_intents.causal`;
- one data-only fragment document;
- a disposable byte-exact clone of the eight pinned O1-O core files;
- the seven exact imported `jellyfish`/`msgpack` dependency files through the
  pinned runtime path.

The original O1-O repository path is never disclosed to the child. Python runs
with `-I -B -S`, a three-variable sanitized environment, no site bootstrap and a
35-second self-timeout. Imported origins and hashes are attested in-process. Both
children must select one identical triplet/fragment, emit byte-identical source
and preserve their complete disposable fixture snapshots. The generated source
must contain one exact literal `NEXT_OPERATOR_JSON` assignment and one zero-arg
reader function. It is syntax-parsed only; bytecode compilation and execution are
structurally zero.

An exclusive `flock` owns the entire O1C-0023 lifecycle. The descriptor is passed
to native children and never explicitly unlocked, so a killed parent cannot be
mistaken for a recoverable idle run while an orphan child remains alive. The lock
releases only after the final inherited descriptor closes.

## Accounting and crash truth

The structural work ledger records started, returned and validated native calls,
AST parses, compile/execute counts, fresh targets, solver, entropy, accelerators
and sibling mutation evidence. Zero sibling writes is emitted only after the
before/after core and complete sibling-tree snapshots match. An unavailable
audit emits `null`, not a fabricated zero; a successfully observed mismatch emits
only a lower bound of one mutation.

The accounting baseline is captured immediately after the exclusive lease, so
strict config loading, the full 384-artifact preflight, Git/source verification,
composition, persistence and both native children all enter CPU and wall budgets.
CPU enforcement is parent process CPU plus the delta of `RUSAGE_CHILDREN`.
Resident-memory enforcement takes the maximum of parent RSS, child rusage RSS and
the two native self-reports. Wall time already encloses both children. A prepared
capsule publishes without recomputation even if its source config later moves or
drifts; its frozen status controls the return code. A running interrupted capsule
is stopped under the execution lease, and recovery claims only checkpoint-bound
facts. Prelaunch native counts are explicitly upper/lower bounds rather than
pretending that durable checkpoint entry proves a child launched. Operational
failure capsules retain the same CPU, wall, RSS, persistent-byte and source-read
telemetry rather than dropping the failed work from the ledger.

## Frozen budgets

- CPU: 30 seconds including native children
- wall: 60 seconds
- resident memory: 512 MiB including native children
- persistent output: 2 MiB
- upstream artifact reads: 64 MiB
- native O1-O invocations: exactly 2
- generated source: at most 64 KiB
- fresh targets, native solver branches, scientific entropy, sibling writes,
  MPS and GPU calls: exactly 0

## Verification at source freeze

- 32 focused tests pass; one optional environment-driven integration test skips.
- The exact synthetic O1C-0022 fixture contains 384 artifacts and four valid K256
  state/ledger/execution chains.
- Negative tests reject fabricated manager authority, a minimal capsule, corrupt
  unused artifacts, rehashed post-freeze mutation and AST-marker tampering.
- Recovery tests cover completed/failed/stopped prepared publication with a
  missing config, an active execution lease, reservation checkpoints, both native
  phases, missing ledgers, snapshot mismatch and unavailable strict config.
- The real local native double assembly passes in disposable directories with
  byte-identical generated source, exact 8+7 module attestation, zero fixture or
  sibling mutation, zero compile and zero execution.
- The real lab preflight returns `prerequisite-pending`, verifies O1-O source pins
  and creates no O1C-0023 reservation because O1C-0022 is not finalized.

## Next transition

Do nothing heavy while W52 owns the resource gate. O1C-0019 must finalize first;
then source-frozen O1C-0022 executes exactly once. Only its authoritative finalized
capsule unlocks O1C-0023. O1C-0023 then records one retrospective decision and
native assembly. The selected science runs only under a new attempt identity and
new source freeze.
