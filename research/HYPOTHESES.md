# Hypothesis Registry

Hypotheses receive stable IDs. Status values are `OPEN`, `ACTIVE`, `SUPPORTED`,
`REFUTED`, or `PARKED`.

| ID | Status | Priority | Hypothesis | Cheapest discriminating test | Success signal | Failure pivot |
|---|---|---:|---|---|---|---|
| `H-EIGHTBLOCK-038` | `REFUTED` | 0 | A bounded O1 stream over eight raw public ChaCha blocks predicts A526 fixed bits better than the random baseline on unseen keys | O1C-0036 trained on 1,024 uniform keys and evaluated 128 disjoint sibling BUILD keys with all 210 published complement bits removed from deployment | Positive held-out compression or a true complement in the exact top-65,536 frontier | Mean `102.5/204`, accuracy `50.2451%`, compression `-0.393341` bit, exact beam `0/128`; close raw-output scaling and move to joint attacker-computable constraints |
| `H-RELATIONAL-037` | `ACTIVE` | 0 | Weak O1 unary/joint scores become useful when coupled to target-specific exact ChaCha search as a joint configuration problem, before 204 coordinate decisions are individually exact | O1C-0044 prospectively ranks fresh truth 54/4097; O1C-0048 restores primary-over-rotation specificity with pair envelopes but its frozen all-arm gate remains negative | Transferable held-out entropy, joint-rank, residual-width or time-to-hit reduction; exact beam inclusion is the highest but not the only pass level | Preserve the complete-state score and let attacker-visible live search outcomes assign bounded credit to competing group operators |
| `H-ALLVAR-SCHEDULER-046` | `REFUTED` | 0 | Greedy uniform-marginal decisions over every observed key/internal factor variable preserve O1C-0044's primary target-specific rank advantage | O1C-0045 compares primary and both rotations against internal search at identical 512-conflict budgets | Primary expands residual frontier and strictly beats both rotations in conflicts or width | Primary closes width 9 but rotations do too and are faster (69/129 versus 281 conflicts); generic factor geometry dominates |
| `H-KEYONLY-SCHEDULER-047` | `REFUTED` | 0 | Observing the complete internal trail while externally deciding only key variables preserves the joint reader orientation and leaves native CDCL in control of internal geometry | O1C-0046 reuses O1C-0045 target, potentials, residuals and budgets with only decision-target filtering changed | Primary-specific Full-256/residual-width/conflict gain over internal and both rotations | Primary improves from 152/281 to 43/87 conflicts at residual 8/9, but matched clause rotation remains better at 22/46; close greedy marginals |
| `H-GLOBAL-PREFIX-048` | `REFUTED` | 0 | The prospectively transferred O1C-0044 signal is global over coordinated assignments and can reduce exact work when fixed disjoint key pairs are compared as units rather than collapsed into one-variable uniform marginals | O1C-0048 freezes 63 primary/internal/key-rotation/clause-rotation pair envelopes and compares Full-256 plus residual W8/W9 at 512 conflicts | Primary-specific reduction under the frozen all-arm gate | Full-256 is 0/4 and maxima are 8/9/9/9, so the all-arm gate fails. Static pairs do restore primary-over-rotation ordering; retain that breadcrumb only and move credit assignment into the live stream |
| `H-LOCAL-CUBE-049` | `SUPPORTED` | 0 | The unchanged O1C-0044 complete-state score concentrates truth within nested local residual cubes even when greedy live schedulers erase its orientation | O1C-0047 exhaustively scores all W8/W12/W16 candidates with 240 consumed truth bits fixed | Primary top-256 at W16, strict margin over both rotations, and independent public verification of the beam hit | Achieved: ranks 1/256, 5/4096, 50/65536 versus W16 controls 60592/43059; 10.356 bits privileged local compression. Ceiling only, not Full-256 recovery |
| `H-APPLE-FIXEDPOINT-050` | `REFUTED` | 0 | The public ChaCha feed-forward fixed-point map or output-Hamming local fitness provides transferable direction toward a uniformly random Full-256 key | APPLE-VIEW-0001 tests 32 deterministic targets, 8,192 one-bit flips, 128 projection chains and six coordinate descents | Positive held-out keybit gain, landscape AUC above chance and exact verified recovery | -0.484 gained key bits, AUC 0.50572, direction accuracy 0.49854, zero recoveries; output error falls without key-distance improvement. Close this path |
| `H-PAIR-SPECIFICITY-051` | `SUPPORTED` | 0 | Complete-state criticality retains reader orientation when converted into reversible pairwise max envelopes even if the resulting static scheduler does not yet reduce absolute work | Consumed O1C-0048 compares the primary pair arm with key- and clause-rotated controls on identical W8/W9 residuals | Primary lexicographically beats each rotation by width then conflicts | Achieved as a post-result diagnostic: primary reaches W9 and uses 75/155 conflicts versus key 195/331 and clause 89/167. This is a consumed specificity breadcrumb, not a promoted attacker-valid result |
| `H-APPLE-CARRY-052` | `REFUTED` | 0 | Quotienting independently lifted addition carries out of the complete Full-256 ChaCha relation exposes exact linear key information | APPLE-VIEW-0002 computes the exact GF(2) key/carry affine system for eight deterministic Full-256 targets | Positive exact key rank, reusable key parity or exact recovery | Carry rank is 512, exact key rank is 0 and recovery is 0/8; independent carries erase every linear key parity. Restore the actual nonlinear carry recurrence by depth or close algebraic quotienting |
| `H-ONLINE-GROUP-CREDIT-053` | `REFUTED` | 0 | A decision-local bounded credit ticket can assign useful target-time outcomes to frozen criticality groups before the next external decision | O1C-0049 runs one Full-256 plus W8/W9/W10 online calls against frozen static primary baselines at the same 512-conflict cap | Strict exact frontier/conflict improvement over static primary | W8/W9 improve 75/155→65/128, but W10 regresses 310→320 and Full-256 is unchanged. Every Full-256 ticket expires before all 590 later backtracks, so close this exact short-horizon update |
| `H-APPLE-CARRY-DEPTH-054` | `REFUTED` | 0 | Restoring the real carry-majority recurrence uniformly from low to high bit positions across all 336 additions exposes a cheap determined-output filter before complete ChaCha evaluation | APPLE-VIEW-0003 forwards exact three-valued carry depths 0..31 through one Full256 block and scores 32 output-independent wrong keys | Any exact wrong-key rejection at depth below 31 without rejecting truth | Depths 0..30 determine zero final output bits and reject 0/32 probes; depth 31 is the full exact computation. Close bitwise ternary carry-depth truncation and require correlation-preserving state or a different computational cut |
| `H-DELAYED-GROUP-CREDIT-055` | `SUPPORTED` | 0 | A bounded eligibility trace that survives decision advance can assign later conflict/backtrack outcomes to the pair-group decisions actually undone, avoiding O1C-0049's uniform-credit collapse | O1C-0050 changes only the horizon and runs one exact W10 call against static 310 conflicts | Exact W10 recovery with fewer than 310 conflicts | Achieved at 302 conflicts with 1,134 B state, 302 conflict-owner undos and seven differentiated groups; test W11 once |
| `H-APPLE-BIDIRECTIONAL-056` | `REFUTED` | 0 | Exact local propagation from a complete probe key and public output rejects wrong Full-256 keys before every carry identity is restored | APPLE-VIEW-0004 runs generalized arc consistency at depths 24/28/29/30/31 on four fixed wrong probes plus truth control | Any wrong-probe conflict below depth 31 with truth retained | Depth 30 infers thousands of internal variables but rejects 0/4; one free high carry per each of 336 additions absorbs local contradictions. Close local propagation depth |
| `H-APPLE-SPARSE-CARRY-057` | `SUPPORTED` | 0 | A small joined subset of the 336 missing high-carry identities forms an exact wrong-key rejection certificate even though uniform depth-30 local propagation does not | APPLE-VIEW-0005 enables those identities as structural/online-ordered switches without CDCL branching and independently replays each reason-DAG slice | Exact rejection of at least one fixed wrong probe with materially fewer than 336 identities and no truth conflict | Achieved in 20/20 runs: slices use 250–265 identities, best 250 with 86 absent; all truth controls complete. Transfer proof relevance next |
| `H-APPLE-PROOF-CREDIT-058` | `SUPPORTED` | 0 | Carry identities that repeatedly participate in exact wrong-key proofs transfer across targets and can be retained as a bounded O1 attention state | APPLE-VIEW-0006 streams 4,603 BUILD proof events once into a 1,346-byte frequency/recency state, freezes it, then scores four disjoint held-out wrong probes first | Strict held-out first-conflict/certificate/work gain over the best fixed structural order with exact replay and truth retention | Certificate arm passes 4/4: learned 248/248/251/250 vs best fixed 251/252/257/255, aggregate 997 vs 1,015 and immediate-public 1,013. Raw scheduling loses 1,268 vs 1,031 switches; support is proof relevance only |
| `H-DELAYED-W11-059` | `REFUTED` | 0 | The O1C-0050 delayed owner rule expands exact completion beyond the frozen W10 frontier without any parameter change | O1C-0051 runs exactly one delayed-primary W11 call at the same 512-conflict cap | Exact publicly verified W11 key; telemetry and wall cannot pass | `UNKNOWN` at 512 conflicts/513 decisions; no controls or Full256 calls authorized. Close delayed unary credit on these pair groups |
| `H-APPLE-PROOF-EDGE-060` | `ACTIVE` | 0 | Bounded proof-edge or predecessor credit preserves the conflict-closing sequence that unary proof-participation counts discard | Stream exact BUILD reason-DAG predecessor events into a fixed target-independent addressed state, freeze one order before disjoint EVAL and make no held-out updates | Strictly fewer aggregate held-out first-conflict switches than both APPLE-VIEW-0006's frozen unary order and the best fixed structural comparator at matched work; certificate-only improvement cannot pass | APPLE-VIEW-0006 proves unary membership transfers but reaches conflict at 317 switches on every held-out probe. Do not refit frequency/recency counts; the next variable is relational sequence only |
| `H-CONTEXT-ACTION-CREDIT-061` | `ACTIVE` | 0 | Pair-group credit becomes frontier-useful only when it distinguishes the action/polarity selected inside each group and its bounded context | Freeze bounded per-pattern credit over the same 63 groups, beginning with exactly `4×63` action cells, then run one consumed primary W11 call at the unchanged 512-conflict cap | Exact publicly verified W11 key; only exact completion can authorize matched controls or Full256, and telemetry/wall cannot pass | O1C-0051 penalizes `(59,60)` on 382 conflict-owner undos, but all four masks share one credit and 502/513 decisions repeat. Failure closes live credit on these fixed pairs; do not tune groups, cap, weights or assume a static predecessor edge |
| `H-PROOF-RELATION-041` | `SUPPORTED` | 0 | H16 branch-difference clause multisets contain portable target-specific signed key-to-internal relations without target-key input | O1C-0039 froze H16/`|J|=0.5` on four BUILD keys, then opened two DEVELOPMENT labels only after both bounded fields and search arms froze | Above-chance primary relation accuracy on both targets and a pooled margin over key- and factor-rotated endpoint controls | Achieved: 55.09%/56.99%, pooled 55.84% versus 52.88%/49.51%; preserve exact field and test aggregate candidate rank rather than resweeping extraction |
| `H-FACTOR-SEARCH-042` | `REFUTED` | 0 | Aggregating the transferred relation field over a complete attacker-computable forward execution ranks the true key and can become reversible live CDCL guidance | O1C-0040 scores 4,096 deterministic decoys per consumed DEVELOPMENT target with raw and one frozen structural-surprise objective | Positive true-key rank on both targets plus geometric margin over endpoint rotations | Raw ranks 1905/4097 and 2292/4097; surprise 1078/4097 and 1461/4097, dominated by key rotation 107/4097 and 423/4097. Close occurrence scoring |
| `H-ANTECEDENT-043` | `SUPPORTED` | 0 | Branch-exclusive signed proof-antecedent chains retain target-dependent causal path identity that terminal clause occurrence collapses into universal ChaCha geometry | O1C-0043 consumed ranks 5/91/141 and O1C-0044 fresh rank 54/4097 with both rotations below truth | Fresh primary rank below 0.25 and strict margin over both endpoint rotations | Achieved; next test consequence in matched exact search rather than another representation panel |
| `H-CHAIN-LEAF-044` | `REFUTED` | 0 | A global `-1` weighted sum over unique signed leaves of branch-exclusive H16 proof chains transfers complete-key rank concentration | O1C-0042 froze the exact O1C-0041 extractor and scorer before one sealed target | Fresh primary rank below 0.25 and above both rotations | Primary 1371/4097 keeps a small control margin but fails 0.25; close without retry and preserve parent role/criticality |
| `H-PARENT-CRITICALITY-045` | `SUPPORTED` | 0 | A bounded BUILD-fitted reader over ordered RUP parent role, original functional-clause criticality, expected pivot and unique-literal phase transfers complete-key joint rank | O1C-0044 freezes exact O1C-0043 reader before one sealed target | One sealed fresh rank below 0.25 and strict margin over key/clause rotations | Achieved at 54/4097 versus rotations 3567/2972; preserve exact reader for live-search consequence |
| `H-KEYGUIDE-039` | `REFUTED` | 0 | A frozen O1 unary field reduces exact full-round search when supplied as confidence-ordered reversible key decisions | O1C-0037 compared internal, phase, real O1, coordinate-shuffled and exact/one-error ceilings on consumed `build-0000` | Less matched search work than both internal and shuffled, or repair of one wrong hint within 32,768 conflicts | Real O1 K256 is 2.123x internal wall and telemetry-identical to shuffled; one wrong hint is unresolved through 32,768 conflicts. Close key-phase-only coupling and move decisions inside relation/proof structure |
| `H-RESIDUAL-040` | `SUPPORTED` | 0 | The unchanged exact ChaCha20-R20 relation can complete a nonzero O1-ordered residual once every supplied prefix decision is correct | O1C-0038 used exact post-reveal signs and frozen O1 confidence order at residual widths 0/1/2/4/8/9/16 | Exact public verification at a nonzero residual width under a bounded conflict budget | Width 8 recovers in 89 conflicts / 135,441 us; width 9 remains unresolved through 32,768 conflicts. Retain this as a decoder ceiling, never as attacker-valid recovered bits |
| `H-TRANSFER-036` | `REFUTED` | 1 | A448's frozen proof-antecedent top4 reader plus exact A442 Borda backbone retains better-than-median byte-3 ordering when the other 248 key bits are unknown | Completed O1C-0031 on consumed RFC8439, then O1C-0032 unchanged on disjoint consumed DEVELOPMENT-0000 | Rank `<=128/256` on both consumed targets, followed only then by one fresh blind target | RFC rank `47` did not repeat: DEVELOPMENT rank `239`; baseline/proof/hybrid were `242/236/239`. Close without spending a fresh target or resweeping any operator, byte, sign or coefficient |
| `H-TRANSFER-034` | `REFUTED` | 1 | A291/A296's frozen shallow conflict/XOR reader retains some byte-value ordering when its exact H1/2/4/8 RawCell field is measured on an all-256 public target with the other 248 key bits unknown | Completed literal byte-2 transfer on RFC plus two consumed DEVELOPMENT targets, then one parameter-unchanged fresh EVALUATION target | Better-than-uniform byte ranks on the consumed panel followed by a positive fresh target | Consumed ranks `118/61/9` failed to generalize: fresh rank `230/256`, four-target rank-product `p=0.1766`. Close without byte/sign/coefficient resweeps; retain only the exact 46–56 s cube adapter |
| `H-RESIDUAL-035` | `ACTIVE` | 0 | The sibling A526/W52 recovery engine can close a full256 O1 posterior once its native 204-bit complement is exact or appears in a tractably small beam | O1C-0035 now applies an exact top-K decoder only to coordinates 52..255 and emits the existing A526 handoff without changing the backend | A526 requires `204/204`, or an explicitly bounded completion beam containing an exact member; a backend hit must pass independent public ChaCha verification | Literal bridge is complete. Current O1C-0022 top-65,536 beams contain `0/20` exact complements; MAP max is `118/204`, post-reveal oracle max `123/204`. Keep decoder/backend frozen and change only upstream eight-block public evidence |
| `H-TRANSFER-037` | `REFUTED` | 0 | A465's exact cubic rank-PoE preserves A448's apparent RFC byte advantage after the known complement is removed | Apply A460/A462/A463 and frozen `(7,1,4)` A465 fusion to both retained all256 fields without new solver work | Both consumed truth bytes better than median before any fresh target | A465 leaves ranks at `47/239`; close the exact transfer and do not resweep |
| `H-TRANSFER-038` | `REFUTED` | 0 | A469's exact positive bucket-local interaction rescues the all256 A465 field | Apply the frozen A469 correction to both prediction-frozen A465 fields without labels or refit | Both consumed ranks improve before any fresh target | Ranks become `56/239`; close the exact transfer and do not resweep |
| `H-TRANSFER-039` | `REFUTED` | 0 | Direct terminal assumptions, failed cores, inverse fixed points or local W8 relaxation around an O1 candidate expose a reusable pre-W52 error-bit field | Completed cheap consumed screens and one unchanged repeat where a breadcrumb appeared | Repeated per-bit correction or stable group correlation that reduces complement Hamming error | Cores remain 255..256 bits; inverse ranking is chance; frozen correction loses 14 DEVELOPMENT bits; neighbor correction splits -4/+6; W8 correlation collapses -0.158 to -0.014. Close these exact surfaces and require a genuinely new raw source |
| `H-LIVE-009` | `REFUTED` | 1 | The O1C-0009 linear full-256 direct/candidate contrast readers remove reproducible code length from a uniform unseen ChaCha20 key | Completed on 128 broker-secret keys with all predictions frozen before reveal, then its only signed breadcrumb replicated on 2,048 more | Held-out total NLL stably below 256 bits with matched controls at chance | O1C-0009 froze at zero and O1C-0010 signed direct reached -0.019088 bit; raw end-output regression is closed |
| `H-SIGNED-013` | `REFUTED` | 1 | O1C-0009's CAL-only negative direct scale reflects a tiny transferable orientation rather than panel noise | O1C-0010 froze exact model/scale and evaluated 2,048 new broker-secret keys without refit | Positive mean compression, conditional and target-level evidence, plus matched frozen-control margins | Direct compression was -0.019088 bit, z -0.946, shuffled margin -0.017541 and output-permutation margin +0.000962; do not revisit |
| `H-CNF-015` | `SUPPORTED` | 1 | A target-independent exact symbolic-output ChaCha20 CNF can expose an attacker-valid paired-assumption relation without retaining a target trace or reducing key width | O1C-0011 compiled, double-reproduced and solver-checked the complete one-block relation before reader training | Exact SAT/UNSAT mapping checks, zero target key units, per-bit causal ranges and bounded-memory generation | Achieved: 32,128 vars, 187,370 clauses, 656 x 32 bit ranges; retain immutable template and move event efficacy to H-SOLVER-014 |
| `H-SOLVER-014` | `REFUTED` | 1 | O1C-0019's learned unary paired-assumption packet reader and stationary picker contain portable all256 key orientation | Completed exact four-fold artifact-only BUILD-LOO gate | Positive raw-reader compression over its untrained twin plus positive live-policy compression | Learned policy `-0.271090`; raw learned `+0.312764` loses untrained `+0.371233`. Close this exact unary reader/picker; a future interaction/antecedent source is a new hypothesis, not a rescue fit |
| `H-ORIENT-016` | `REFUTED` | 1 | The O1C-0013 BUILD/CAL procedure identifies a portable global orientation for the O1C-0012 h96 proof-prefix field | Completed exact no-refit sequence across O1C-0013/14/16 | Positive total compression and target robustness on the 32-key panel | O1C-0016 h96 is -0.175 bit/key; pooled exact h96 across 42 keys is -0.08457 with z +0.134; close this orientation |
| `H-POLYPHASE-017` | `REFUTED` | 1 | Equal-logit compounding of h96 and h65 stabilizes the unary inverse direction | O1C-0016 froze exact readers and composition before 32 fresh target entropy calls | Positive ensemble/control margin, at least 18/32 positive, and promotion z >=1.64485 | Ensemble is -0.078249 bit/key, 11/32 positive, paired z -0.555 and promotion z 1.004; do not search weights |
| `H-ONLINE-018` | `SUPPORTED` | 1 | A shared O1 reader can discover an anonymous oriented channel online and an exact bounded 256-coordinate Bit-Vault can retain its addressed readings across a full-width stream without current-target gradient updates | O1C-0017 trained after eight known BUILD freezes, then froze five arms on 16 disjoint synthetic full-256 episodes with zero fresh entropy | Achieved: +42.308742 bits, 80.224609% accuracy, 16/16 positive; +46.701393 over ablation, +42.764897 over shifted labels, +47.231321 over raw O1 end state; every invariant passed | O1C-0020 separately upgrades explicit long-stream retention to exact; real causal orientation and picker agency remain the O1C-0019/O1C-0021 frontier |
| `H-RETENTION-022` | `SUPPORTED` | 1 | A BUILD-trained O1 gate can select 256 public bindings from one unified stream and an exact addressed Bit-Vault can retain them through at least `2^20` distractors without an oracle deployment mask or O(T) model state | O1C-0020 froze 32 BUILD and 8 CAL seeds, then evaluated 4 unseen seeds at H=0/2^16/2^20 with shuffled, untrained, cue, all-open and storage controls | Achieved: 12/12 cells at 256/256, TP=256/FP=FN=0, nested 352-byte state exact, global route margin +0.454628; every longest control fails | Freeze the API; explicit binding values are solved, so move to H-CAUSAL-023 rather than extending the same haystack for vanity |
| `H-CAUSAL-023` | `ACTIVE` | 1 | The same bounded O1 route/vault API can learn reliability-weighted accumulation of weak and contradictory coordinate evidence on unseen 256-bit streams rather than merely store explicit values | O1C-0021 is source-frozen at `4ba1cc6`: after sibling compute clears, repeat the two-seed broad DEV check and then score four never-used 256-bit EVAL episodes once against zero-prior, last, unit-sum, static/current-marker, shuffled, all-open and an independent bounded public-FSM reference | Positive held-out compression in every seed, calibrated evidence growth with stream length, material equal-work control margins and unchanged bounded live state; exact 256/256 is the terminal synthetic gate | If routing works but accumulation fails, change only update/decay/confidence semantics; if routing fails, enrich public event observability before touching real solver streams |
| `H-REAL-VAULT-024` | `REFUTED` | 1 | O1C-0019's frozen incremental q-deltas compound usefully in the exact 352-byte addressed vault as coverage grows from 12 to 256 coordinates | Completed O1C-0022 K=12/52/128/256 replay | Positive K256 compression and exact-complement improvement | Int8 curve falls from `-0.006772` to `-1.181837` bits; raw/normalized/unit-sign K256 are also negative; no backend entry gate passes. Close the packet/vault field without scale, quantizer or reader sweep |
| `H-COMPOSER-025` | `PARKED` | 1 | A complete frozen result can drive O1-O as a deterministic non-repeating experiment composer | O1C-0023 remains source-frozen but unrun | Execute only when a positive evidence field needs autonomous optimization | O1C-0022 is null; composing another operator over it is process work without recovery ROI. Keep the code, spend no run |
| `H-FRONTIER-026` | `SUPPORTED` | 1 | Exact all-coordinate factorized top-K decoding fixes the restricted uncertainty cube's blind spots and gives a reusable full-256 recovery frontier, but useful recovery still requires a concentrated upstream posterior | O1C-0024 exhaustively proved low-width order, recovered a constructed 20-round rank-four key outside the matched local cube, then decoded 65,536 frozen candidates from one burned O1C-0016 posterior | Achieved mechanism: exact deterministic global ordering and public verification; burned diagnostic honestly reports 0 hits, MAP H117 and best H110 at rank 15,405 under all lifecycle/resource gates | Freeze decoder `36133bc`; do not scale K on the opened null. Feed it only a future held-out posterior with portable entropy reduction and add matched search-diversity controls |
| `H-LOGIT-FRONTIER-027` | `SUPPORTED` | 1 | A frozen full-256 natural-logit field can enter the exact global frontier without sigmoid saturation or rounded `/ln2` ordering while remaining bound to its public-target lifecycle | O1C-0025 source `b008e21` covers exact binary64-logit integer ordering, the fixed 57,344 B O1C-0022 tensor to 2,048 B K256 `quantized_int8_vault` slice, the internally verified supplied upstream freeze chain and K=65,536 | Achieved instrument: 14 focused plus 18 neighboring tests pass (32 tests / 80 subtests); 65,536-candidate smoke is 0.937653 s at 44,384,256 B peak | Keep the handoff fixed and unreserved until O1C-0022 supplies positive frozen logits; an instrument pass is not evidence and must not motivate decoder tuning |
| `H-PROOF-ANCESTRY-028` | `PARKED` | 1 | A fixed second-order ancestry-touch x proof-context bias may add portable orientation beyond the frozen O1C-0022 scalar output | Source-frozen proxy v2 `0af57fb` already passes label-free real-FAP structure, exact swap/control and 8-KiB-state tests; only if authoritative finalized O1C-0023 selects `proof_ancestry_pair_residual_v1`, reserve the four-fold residual run on consumed BUILD FAPs | Positive compression and offset improvement in all four folds plus strict margins over all-256 identity shuffle, additive-factorized and common-mode controls | If O1C-0023 selects another operator, do not run it; if selected and null, close only `fap_ancestry_touch_bilinear_proxy_v2`, retain parent R07 and advance to a raw antecedent/signed-pair sensor rather than tuning the decoder |
| `H-PAIR-PROXY-029` | `SUPPORTED` | 1 | A self-preserving 16x8 projection can remain pair-identity-sensitive without giving the factual arm a scale advantage or exceeding bounded deployment state | Completed label-free replay of all four BUILD FAPs with primary and all-256 deranged touch control | Achieved: RMS ratio 1.008770, cosine 0.027591, identical only on 85 branch-empty rows; 1.609594 s, 105,955,328 B process peak; effective reader plus posterior exactly 8,192 B | Structural support is not key orientation; retain the frozen v2 basis and wait for the authoritative residual-label lifecycle |
| `H-HOT-READOUT-030` | `SUPPORTED` | 1 | For a fixed encoder/kernel/phase basis, one T-independent full-256 polyphase state can support multiple late-bound readout weights and temperatures without replay or state mutation | O1C-0027 consumed one deterministic `384x3x256` stream once, queried four immutable readers, compared an independent complex128 recurrence, rechunk/swap controls, a collapsed-bank null and three foreign bases | Achieved: 25,096 B per state; minimum normalized reader RMS 0.081663 versus 1.24e-16 collapsed null; exact rechunk/swap/serialization; zero query reingestion/state writes; every foreign basis rejects | Bind O1-O choices to immutable hot specs and next ingest real O1C-0022 causal packets; any encoder/kernel/phase change remains cold and uses a new attempt |
| `H-HORIZON-HOT-031` | `SUPPORTED` | 1 | A complete O1C-0022-compatible K256 packet ledger can enter one allocation-invariant bounded state in canonical horizon-major order and accept lineage-bound O1-O reader choices as hot parameters without evidence replay | O1C-0028 independently reconstructed the pinned packet ABI, compared normalized/int8 transports and sparse/order controls, then repeated its V2 result in eight fresh processes | Achieved: all 14 gates pass; 25,128 B state, one primary consume, zero primary reingestion, result SHA `ed3517f2...`, 42.8125 MiB peak and 0.143779 s capsule time | Freeze the ABI. After authoritative O1C-0019/22, use O1C-0029 for the real packet stream; O1C-0023 is optional metadata only, while evidence/address/basis changes remain cold |
| `H-STACKED-HOT-032` | `PARKED` | 1 | A hot H64/H65/H96 readout can recover orientation from the real O1C-0022 packet field | O1C-0029 remains source-frozen and unrun | Run only after an upstream field has a positive recovery effect | O1C-0019 learning loses untrained and every O1C-0022 K256 field is negative. A hot readout of the same null evidence is not the next paid experiment |
| `H-DIAGONAL-LAMP-033` | `REFUTED` | 1 | Same-coordinate diagonal self-ancestry innovations become a portable key-orientation lamp when each horizon increment is amplified by its local exact-conflict confidence bit | O1C-0030 completed four BUILD leave-one-out folds on the already-consumed real FAPs with one RMS-standardized global scalar per arm, predictions frozen before each heldout label open, cumulative/legacy/deranged/even controls and exact top-65,536 decoding | Positive primary compression in all four folds, strict improvement over cumulative replacement in all four and a positive margin over fixed deranged confidence | Primary is -0.680620 bit/key, positive in 3/4 but trails cumulative in 4/4 by -0.582832 bit mean; deranged confidence wins at +0.779642 and no exact key enters top 65,536. Close only summarized diagonal self-ancestry plus local hard-q attachment; retain raw antecedent identity, signed interactions, the learned full-330D reader and live global scout-to-focus |
| `H-AGENCY-020` | `REFUTED` | 1 | O1C-0019's target-conditioned preview and learned abstention select more key information than fixed routing | Completed exact four-fold O1C-0019 comparison | Positive final compression and positive margins over hash, shifted and static controls | Learned final mean `-0.271090`; all three IAUC margins are negative; STOP and no-STOP are identical because every fold exhausts the field. Close this picker |
| `H-CARRIER-021` | `REFUTED` | 1 | Separate unary horizon carriers plus incremental gating preserve useful all256 orientation | Completed O1C-0019 and O1C-0022 | Positive learned-reader advantage and positive K256 accumulation | Learned reader loses untrained by `0.058470` bit and all K256 accumulators are negative. Retire global unary packets |
| `H-MOBIUS-019` | `ACTIVE` | 2 | Sparse Boolean Möbius pair interactions cancel common mode and expose carry/proof innovations absent from unary polarity differences | Only after an O1C-0022 all-float null, test projected causally linked signed-variable pairs/proof antecedents on consumed BUILD folds after nuisance centering; compare m_ij with matched balanced rotations at equal work | Cross-key residual orientation and information gain beyond degree-1 under strict held-out folds | If no lift, suppress degree-2 family contextually and retain coverage-debt exploration without exhaustive pair expansion; never fall back to the A539/A541-closed single-position additive marginal |
| `H-CONTRAST-010` | `PARKED` | 3 | Structured one-bit, Gray, repeated-word and posterior-directed candidate traces teach a reusable inverse operator that later transfers to uniform random proposals | O1C-0009 tested the first linear realization | More predictable bits or lower NLL on uniform unseen targets, not only structured keys | Resume only when a solver/carry sensor supplies nonzero local evidence; do not enlarge the same end-output reader |
| `H-TEACHER-011` | `REFUTED` | 3 | O1C-0009's privileged target round/carry auxiliary loss distills an output-only linear inverse without entering deployment inputs | Completed with the same public deployment schema as the relative arm | Distilled arm improves uniform held-out NLL while target-time schema remains public-only | Distilled CAL scale was zero; future teacher labels must supervise an attacker-observable solver event, not raw output alone |
| `H-INTERACT-012` | `PARKED` | 2 | A learned multi-wavelength PoE plus A469 positive bucket-local interaction can improve a stable residual causal posterior | O1C-0018 did not establish a raw unary backbone; O1C-0019 must first correct accumulation and produce a stable packet posterior | Resume only after packetized unary BUILD-LOO lowers NLL; interaction must then improve it without degrading controls | Never place interaction on a null or double-integrated unary field; use identity-preserving residual correction only |
| `H-STAGE3-001` | `ACTIVE` | 1 | Manifest-pinned pre-result solver or trajectory fields contain attacker-computable information that transfers to a held-out target ordering | Build one small frozen dataset and compare factual labels with shuffled/matched controls on disjoint source artifacts | Stable validation lift with identical work and no target-derived inputs | Localize by feature family, then pivot from aggregate metrics to temporal deltas |
| `H-MEM-002` | `SUPPORTED` | 2 | A stream-length-bounded spectral/slot state can retain the useful order of a fixed 4,096-cell Direct12 stream without explicit candidate rows | Compare global sparse Walsh, multi-slot Walsh and quantized Bit-Vault arms under exact state/work accounting | High transfer fidelity with exact state accounting | Achieved mechanistically, but O1C-0006 proves the full-rank bank is table-equivalent and larger than the matched direct table; it is a ceiling, not compression |
| `H-O1O-003` | `ACTIVE` | 2 | Adaptive operator selection gives more validation information per billed work unit than a fixed operator order | Carry the receipt-bound A348-only selection rule across multiple fresh validation fields without consuming their test feedback | Repeated selection of compact high-fidelity mechanisms without deployment feedback | Improve proposal utility with empirical family uncertainty rather than more retries |
| `H-HOLO-004` | `PARKED` | 5 | Structured polyphase or multi-slot binding can exceed equal-cell single-state holography at the same declared budget | Compare mechanistically distinct structured binding against current 83.5938% baseline | Repeated exact-rate or material accuracy gain under matched precision | Do not revisit plain equal-cell superposition; use fixed slots or learned addressing |
| `H-SHAPE-005` | `SUPPORTED` | 1 | Joint temporal profiles and XOR-neighbor/Laplacian geometry transfer more reliably than a validation-selected raw end-horizon channel | Reproduce the independently specified 532-feature transform and frozen A342 pair, then audit it without refitting | Exact independent A348/A349 score and order commitments | The transform is now an input primitive; test bounded retention rather than resweeping readers |
| `H-FRESH-006` | `PARKED` | 4 | The exact compact O1C-0007 decoder preserves useful ordering on completely untouched paired-assumption solver trajectories and improves search under equal work | Superseded as the primary next test by the full-256 Living Inverse; the frozen decoder remains available as a matched unary arm | Only useful if it lowers NLL on a full-256 public-output target | Do not generate a narrow W46 target merely to complete this historical plan |
| `H-COMPACT-007` | `ACTIVE` | 1 | Upstream bit/carry/solver events contain compressible residual structure that a bounded non-dictionary O1 state can accumulate even though their global difficulty field lacks key orientation | O1C-0016 validates 67,584-byte lifecycle but closes its fixed readout | Cross-fitted nuisance-rejected NLL lift with exact state/work accounting | Preserve bounded state; move to provenance-localized query cones if residual efficacy fails |
| `H-PAIR-008` | `ACTIVE` | 2 | Paired coordinate interventions expose reusable causal innovations after common-mode removal even though their raw global proof difficulty is non-orienting | O1C-0022 first tests the frozen incremental packet reader; the predeclared fallback preserves projected clause/proof identity and compares pair/antecedent/contradiction structure on consumed BUILD folds | Cross-key residual NLL against both common-mode and coordinate-destroying controls | Localize by assumption-rooted proof/carry ancestry and sparse Möbius pairs rather than revealed coordinate masks or another additive single-position clause reader |

## Leakage kill conditions

`H-RESIDUAL-035` controls only the native A325/A526 branch. The handoff is ready,
but those engines are not authorized until the true key can mathematically exist
in their residual domain. `H-RELATIONAL-037` may instead reduce a joint domain
and use another exact verifier; it must report real entropy/rank/search reduction,
not merely satisfaction of equations already equivalent to the public relation.
Older `ACTIVE` infrastructure, composer and packet-chain rows remain background
assets; they do not turn a wrong fixed complement into recovery evidence.

Any feature is excluded from discovery if it depends on the recovered target key,
post-result target rank, target-only internal state, outcome-conditioned file
selection or information unavailable under the billed attacker computation.
