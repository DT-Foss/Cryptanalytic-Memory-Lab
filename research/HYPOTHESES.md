# Hypothesis Registry

Hypotheses receive stable IDs. Status values are `OPEN`, `ACTIVE`, `SUPPORTED`,
`REFUTED`, or `PARKED`.

| ID | Status | Priority | Hypothesis | Cheapest discriminating test | Success signal | Failure pivot |
|---|---|---:|---|---|---|---|
| `H-LIVE-009` | `ACTIVE` | 1 | A full-256 attacker-valid contrast stream can remove reproducible code length from a uniform unseen ChaCha20 key without retaining candidate rows | Train a small CPU reader on known full-256 keys, then evaluate key NLL on a fixed disjoint uniform development corpus using only public target output and self-generated candidate traces | Held-out total NLL is stably below 256 bits with matched controls at chance | Split direct-output, candidate-residual and trace-derived channels; retain the exact round/orbit where information disappears |
| `H-CONTRAST-010` | `ACTIVE` | 1 | Structured one-bit, Gray, repeated-word and posterior-directed candidate traces teach a reusable inverse operator that later transfers to uniform random proposals | Compare the same reader trained with and without the structured-to-uniform curriculum at equal cipher calls | More predictable bits or lower NLL on uniform unseen targets, not only structured keys | Increase proposal diversity and learn local correction only where posterior anchors are close; never redefine the target distribution |
| `H-TEACHER-011` | `ACTIVE` | 1 | Privileged target round/carry/proof labels can distill an output-only inverse without entering deployment inputs | Use identical public input for direct and teacher-distilled students and mechanically reject teacher fields in the deployment dataclass | Distilled arm improves uniform held-out NLL while target-time schema remains public-only | Localize auxiliary loss by round, carry frontier and lane; remove teacher tasks that do not affect public-output generalization |
| `H-INTERACT-012` | `ACTIVE` | 1 | A465 multi-wavelength PoE plus A469 positive bucket-local interaction preserves a strong unary posterior while extracting sparse cross-bit evidence | Compare unary, PoE and PoE+local-correction arms in the same 20,492-byte state budget | PoE+local improves minimum block rank/NLL without degrading most bits or controls | Keep A465 identity backbone; restrict correction support by confidence, ARX edge and surprise rather than trying another global copula |
| `H-STAGE3-001` | `ACTIVE` | 1 | Manifest-pinned pre-result solver or trajectory fields contain attacker-computable information that transfers to a held-out target ordering | Build one small frozen dataset and compare factual labels with shuffled/matched controls on disjoint source artifacts | Stable validation lift with identical work and no target-derived inputs | Localize by feature family, then pivot from aggregate metrics to temporal deltas |
| `H-MEM-002` | `SUPPORTED` | 2 | A stream-length-bounded spectral/slot state can retain the useful order of a fixed 4,096-cell Direct12 stream without explicit candidate rows | Compare global sparse Walsh, multi-slot Walsh and quantized Bit-Vault arms under exact state/work accounting | High transfer fidelity with exact state accounting | Achieved mechanistically, but O1C-0006 proves the full-rank bank is table-equivalent and larger than the matched direct table; it is a ceiling, not compression |
| `H-O1O-003` | `ACTIVE` | 2 | Adaptive operator selection gives more validation information per billed work unit than a fixed operator order | Carry the receipt-bound A348-only selection rule across multiple fresh validation fields without consuming their test feedback | Repeated selection of compact high-fidelity mechanisms without deployment feedback | Improve proposal utility with empirical family uncertainty rather than more retries |
| `H-HOLO-004` | `PARKED` | 5 | Structured polyphase or multi-slot binding can exceed equal-cell single-state holography at the same declared budget | Compare mechanistically distinct structured binding against current 83.5938% baseline | Repeated exact-rate or material accuracy gain under matched precision | Do not revisit plain equal-cell superposition; use fixed slots or learned addressing |
| `H-SHAPE-005` | `SUPPORTED` | 1 | Joint temporal profiles and XOR-neighbor/Laplacian geometry transfer more reliably than a validation-selected raw end-horizon channel | Reproduce the independently specified 532-feature transform and frozen A342 pair, then audit it without refitting | Exact independent A348/A349 score and order commitments | The transform is now an input primitive; test bounded retention rather than resweeping readers |
| `H-FRESH-006` | `PARKED` | 4 | The exact compact O1C-0007 decoder preserves useful ordering on completely untouched paired-assumption solver trajectories and improves search under equal work | Superseded as the primary next test by the full-256 Living Inverse; the frozen decoder remains available as a matched unary arm | Only useful if it lowers NLL on a full-256 public-output target | Do not generate a narrow W46 target merely to complete this historical plan |
| `H-COMPACT-007` | `ACTIVE` | 1 | Upstream bit/carry/solver evidence contains compressible structure that a bounded non-dictionary O1 state can accumulate even though the final full-round scalar score field is spectrally dense | O1C-0007 already passed the structural gate at 12 registers/266 bytes; O1C-0008 now tests the same frozen mechanism on disjoint source trajectories | Stable fresh transfer and material fidelity or time-to-hit advantage with exact state/work accounting | Do not reinterpret the negative A355 calibration; if unary evidence fails freshly, add provenance-localized interactions rather than candidate rows |
| `H-PAIR-008` | `ACTIVE` | 2 | Paired candidate interventions under explicit coordinate flips expose more reusable bit evidence than aggregate full-round score distance | On full-256 known keys, stream matched self-generated `k_i` flip pairs and compare signed reader events against orientation-swapped and output-flip controls | Cross-key sign consistency and lower held-out bit NLL under the public-output deployment API | Split by round, carry, proof ancestry and proposal distance; raw ciphertext distance remains a control |

## Leakage kill conditions

Any feature is excluded from discovery if it depends on the recovered target key,
post-result target rank, target-only internal state, outcome-conditioned file
selection or information unavailable under the billed attacker computation.
