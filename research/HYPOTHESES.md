# Hypothesis Registry

Hypotheses receive stable IDs. Status values are `OPEN`, `ACTIVE`, `SUPPORTED`,
`REFUTED`, `INCONCLUSIVE`, or `PARKED`.

| ID | Status | Priority | Hypothesis | Cheapest discriminating test | Success signal | Failure pivot |
|---|---|---:|---|---|---|---|
| `H-EIGHTBLOCK-038` | `REFUTED` | 0 | A bounded O1 stream over eight raw public ChaCha blocks predicts A526 fixed bits better than the random baseline on unseen keys | O1C-0036 trained on 1,024 uniform keys and evaluated 128 disjoint sibling BUILD keys with all 210 published complement bits removed from deployment | Positive held-out compression or a true complement in the exact top-65,536 frontier | Mean `102.5/204`, accuracy `50.2451%`, compression `-0.393341` bit, exact beam `0/128`; close raw-output scaling and move to joint attacker-computable constraints |
| `H-RELATIONAL-037` | `ACTIVE` | 0 | Weak O1 unary/joint scores become useful when coupled to target-specific exact ChaCha search as a joint configuration problem, before 204 coordinate decisions are individually exact | O1C-0082 emits 257 globally novel threshold no-goods; O1C-0085 adds 23 on Page 10; O1C-0086 then adds 202 more on Page 11 against the complete 830-clause baseline at `128/131/131` work | Further global novelty, certified closure/model, attacker-valid entropy/domain reduction or publicly verified key; exact recovery remains the highest gate | Sub-recovery compounding is strongly supported, recovery remains open. Roll all 202 O1C-0086 clauses and evolved `658fd285…` bank into fresh Page 12; keep cap/RAM and Page-8/9/10/11 replay closed |
| `H-ALLVAR-SCHEDULER-046` | `REFUTED` | 0 | Greedy uniform-marginal decisions over every observed key/internal factor variable preserve O1C-0044's primary target-specific rank advantage | O1C-0045 compares primary and both rotations against internal search at identical 512-conflict budgets | Primary expands residual frontier and strictly beats both rotations in conflicts or width | Primary closes width 9 but rotations do too and are faster (69/129 versus 281 conflicts); generic factor geometry dominates |
| `H-KEYONLY-SCHEDULER-047` | `REFUTED` | 0 | Observing the complete internal trail while externally deciding only key variables preserves the joint reader orientation and leaves native CDCL in control of internal geometry | O1C-0046 reuses O1C-0045 target, potentials, residuals and budgets with only decision-target filtering changed | Primary-specific Full-256/residual-width/conflict gain over internal and both rotations | Primary improves from 152/281 to 43/87 conflicts at residual 8/9, but matched clause rotation remains better at 22/46; close greedy marginals |
| `H-GLOBAL-PREFIX-048` | `REFUTED` | 0 | The prospectively transferred O1C-0044 signal is global over coordinated assignments and can reduce exact work when fixed disjoint key pairs are compared as units rather than collapsed into one-variable uniform marginals | O1C-0048 freezes 63 primary/internal/key-rotation/clause-rotation pair envelopes and compares Full-256 plus residual W8/W9 at 512 conflicts | Primary-specific reduction under the frozen all-arm gate | Full-256 is 0/4 and maxima are 8/9/9/9, so the all-arm gate fails. Static pairs do restore primary-over-rotation ordering; retain that breadcrumb only and move credit assignment into the live stream |
| `H-LOCAL-CUBE-049` | `SUPPORTED` | 0 | The unchanged O1C-0044 complete-state score concentrates truth within nested local residual cubes even when greedy live schedulers erase its orientation | O1C-0047 exhaustively scores all W8/W12/W16 candidates with 240 consumed truth bits fixed | Primary top-256 at W16, strict margin over both rotations, and independent public verification of the beam hit | Achieved: ranks 1/256, 5/4096, 50/65536 versus W16 controls 60592/43059; 10.356 bits privileged local compression. Ceiling only, not Full-256 recovery |
| `H-APPLE-FIXEDPOINT-050` | `REFUTED` | 0 | The public ChaCha feed-forward fixed-point map or output-Hamming local fitness provides transferable direction toward a uniformly random Full-256 key | APPLE-VIEW-0001 tests 32 deterministic targets, 8,192 one-bit flips, 128 projection chains and six coordinate descents | Positive held-out keybit gain, landscape AUC above chance and exact verified recovery | -0.484 gained key bits, AUC 0.50572, direction accuracy 0.49854, zero recoveries; output error falls without key-distance improvement. Close this path |
| `H-PAIR-SPECIFICITY-051` | `SUPPORTED` | 0 | Complete-state criticality retains reader orientation when converted into reversible pairwise max envelopes even if the resulting static scheduler does not yet reduce absolute work | Consumed O1C-0048 compares the primary pair arm with key- and clause-rotated controls on identical W8/W9 residuals | Primary lexicographically beats each rotation by width then conflicts | Achieved as a post-result diagnostic: primary reaches W9 and uses 75/155 conflicts versus key 195/331 and clause 89/167. This is a consumed specificity breadcrumb, not a promoted attacker-valid result |
| `H-APPLE-CARRY-052` | `REFUTED` | 0 | Quotienting independently lifted addition carries out of the complete Full-256 ChaCha relation exposes exact linear key information | APPLE-VIEW-0002 computes the exact GF(2) key/carry affine system for eight deterministic Full-256 targets | Positive exact key rank, reusable key parity or exact recovery | Carry rank is 512, exact key rank is 0 and recovery is 0/8; independent carries erase every linear key parity. Restore the actual nonlinear carry recurrence by depth or close algebraic quotienting |
| `H-ONLINE-GROUP-CREDIT-053` | `REFUTED` | 0 | A decision-local bounded credit ticket can assign useful target-time outcomes to frozen criticality groups before the next external decision | O1C-0049 runs one Full-256 plus W8/W9/W10 online calls against frozen static primary baselines at the same 512-conflict cap | Strict exact frontier/conflict improvement over static primary | W8/W9 improve 75/155→65/128, but W10 regresses 310→320 and Full-256 is unchanged. Every Full-256 ticket expires before all 590 later backtracks, so close this exact short-horizon update |
| `H-APPLE-CARRY-DEPTH-054` | `REFUTED` | 0 | Restoring the real carry-majority recurrence uniformly from low to high bit positions across all 336 additions exposes a cheap determined-output filter before complete ChaCha evaluation | APPLE-VIEW-0003 forwards exact three-valued carry depths 0..31 through one Full256 block and scores 32 output-independent wrong keys | Any exact wrong-key rejection at depth below 31 without rejecting truth | Depths 0..30 determine zero final output bits and reject 0/32 probes; depth 31 is the full exact computation. Close bitwise ternary carry-depth truncation and require correlation-preserving state or a different computational cut |
| `H-DELAYED-GROUP-CREDIT-055` | `SUPPORTED` | 0 | A bounded eligibility trace that survives decision advance can assign later conflict/backtrack outcomes to the pair-group decisions actually undone, avoiding O1C-0049's uniform-credit collapse | O1C-0050 changes only the horizon and runs one exact W10 call against static 310 conflicts | Exact W10 recovery with fewer than 310 conflicts | Achieved at 302 conflicts with 1,134 B state, 302 conflict-owner undos and seven differentiated groups; test W11 once |
| `H-APPLE-BIDIRECTIONAL-056` | `REFUTED` | 0 | Exact local propagation from a complete probe key and public output rejects wrong Full-256 keys before every carry identity is restored | APPLE-VIEW-0004 runs generalized arc consistency at depths 24/28/29/30/31 on four fixed wrong probes plus truth control | Any wrong-probe conflict below depth 31 with truth retained | Depth 30 infers thousands of internal variables but rejects 0/4; one free high carry per each of 336 additions absorbs local contradictions. Close local propagation depth |
| `H-APPLE-SPARSE-CARRY-057` | `SUPPORTED` | 0 | A small joined subset of the 336 missing high-carry identities forms an exact wrong-key rejection certificate even though uniform depth-30 local propagation does not | APPLE-VIEW-0005 enables those identities as structural/online-ordered switches without CDCL branching and independently replays each reason-DAG slice | Exact rejection of at least one fixed wrong probe with materially fewer than 336 identities and no truth conflict | Achieved in 20/20 runs: slices use 250–265 identities, best 250 with 86 absent; all truth controls complete. Transfer proof relevance next |
| `H-APPLE-PROOF-CREDIT-058` | `SUPPORTED` | 0 | Carry identities that repeatedly participate in exact wrong-key proofs transfer across targets and can be retained as a bounded O1 attention state | APPLE-VIEW-0006 streams 4,603 BUILD proof events once into a 1,346-byte frequency/recency state, freezes it, then scores four disjoint held-out wrong probes first | Strict held-out first-conflict/certificate/work gain over the best fixed structural order with exact replay and truth retention | Certificate arm passes 4/4: learned 248/248/251/250 vs best fixed 251/252/257/255, aggregate 997 vs 1,015 and immediate-public 1,013. Raw scheduling loses 1,268 vs 1,031 switches; support is proof relevance only |
| `H-DELAYED-W11-059` | `REFUTED` | 0 | The O1C-0050 delayed owner rule expands exact completion beyond the frozen W10 frontier without any parameter change | O1C-0051 runs exactly one delayed-primary W11 call at the same 512-conflict cap | Exact publicly verified W11 key; telemetry and wall cannot pass | `UNKNOWN` at 512 conflicts/513 decisions; no controls or Full256 calls authorized. Close delayed unary credit on these pair groups |
| `H-APPLE-PROOF-EDGE-060` | `REFUTED` | 0 | Bounded proof-edge or predecessor credit preserves the conflict-closing sequence that unary proof-participation counts discard | APPLE-VIEW-0007 streams exact canonical BUILD predecessor events into a 113,570-byte addressed state, freezes one static strongest-predecessor reader before disjoint EVAL and makes zero held-out updates | Strictly fewer aggregate held-out first-conflict switches than both APPLE-VIEW-0006's frozen unary order and the best fixed structural comparator at matched work; certificate-only improvement cannot pass | Raw edge order is 1,340 vs exact unary 1,268 vs fixed 1,031, so the gate fails. Certificate 1,003 beats fixed 1,015 but loses unary 997 and cannot pass. Close this static/global reader; repeated root 11 has zero edge support and lands at position 335, converging on live action-conditioned context rather than a rescue sweep |
| `H-CONTEXT-ACTION-CREDIT-061` | `REFUTED` | 0 | Pair-group credit becomes frontier-useful when it distinguishes the selected action/polarity inside each group | O1C-0052 freezes exactly `4×63` action cells over the same groups and runs one consumed primary W11 call at the unchanged 512-conflict cap | Exact publicly verified W11 key; telemetry/wall cannot pass | `UNKNOWN` at 512 conflicts/513 decisions. The state reorders 162/448 later selections and differentiates 18 cells, but all visited cells are penalized and 502/513 decisions still repeat. Close negative-only exact-mask credit without tuning |
| `H-DEEPEST-SURVIVOR-SUPPORT-062` | `REFUTED` | 0 | The deepest externally owned action that survives a conflict backjump is a cheap live proxy for the retained causal/proof frontier, whereas the undone action is not localized blame | O1C-0053 reuses the 2,646-byte action/owner layout, gives exactly one `+32` update to the deepest surviving owner on each conflict backjump and runs one frozen primary W11 call at the same groups/seed/512 cap | Exact publicly verified W11 key; telemetry/wall cannot pass | `UNKNOWN` at 512 conflicts/513 decisions/12,068,568 propagations despite 512 updates/16,384 support units; 111 actions reorder, only two groups differentiate and 502 decisions repeat. Post-result truth alignment (4/8 active groups, 9,472/16,384 units) is consumed and does not rescue the gate. Close survival without tuning and move to exact learned-clause/first-UIP antecedent membership |
| `H-EXACT-CONFLICT-ANTECEDENT-063` | `REFUTED` | 0 | Exact learned-clause membership plus equal negative credit to every represented live owner localizes contradiction blame better than undo or trail survival | O1C-0055 applies exactly `-32` once per distinct represented `(group,mask)` in each minimized learned clause under the frozen W11 gate | Exact publicly verified W11 key; telemetry alone cannot pass | `UNKNOWN` at 512 conflicts despite membership in 512/512 clauses. 2,057 penalties collapse onto the same 18 cells/seven groups as O1C-0052; 502 decisions repeat. Close all-member credit without tuning |
| `H-GLOBAL-FACTOR-BOUND-064` | `REFUTED` | 0 | Summing exact conditional maxima of each O1C-0044 factor yields an admissible prefix bound tight enough for a bounded global Full256 beam and certified residual queue | O1C-0054 runs one unconditional public-only width-256 Full256 beam before reveal, then one frozen post-reveal W11 best-first diagnostic | Exact publicly verified Full256 key, otherwise a certified W11 top-five containing truth | Full256 recovers no key, loses truth at stage 5 and ends at top/min Hamming 120/116; W11 reaches 1,024 pops/14 forwards with zero certified leaves. Exact W12 rank 5 does not survive independent factor maxima. Close without width/order/cap/scale tuning |
| `H-CLAUSE-ROLE-CREDIT-065` | `REFUTED` | 0 | Selecting one exact learned-clause member by deepest/current conflict role and assigning fixed `-32` avoids O1C-0055's collective blame strongly enough to close W11 | O1C-0056 freezes one deepest/current-level live-owner selector with unchanged groups/state scale/seed/W11 cap and executes one efficacy call | Exact publicly verified W11 key under the frozen gate; telemetry alone cannot pass | `UNKNOWN` at 512 conflicts despite exact one-role selection in 512/512 clauses. All selections are unique/current-level, but the state still occupies 18 cells/seven groups and 502 decisions repeat. Refute only fixed negative `-32`; do not tune sign/scale/cap or revisit localization |
| `H-UTILITY-CLAUSE-ROLE-066` | `PARKED` | 1 | The exact unique current-level clause role retained by O1C-0056 becomes useful only when credit sign or magnitude is conditioned on an attacker-visible outcome/utility signal rather than conflict occurrence alone | Keep parked behind the cheaper confidence-ranked vault `cb_decide` successor; if resumed later, precommit one bounded live utility event and one matched W11 comparison without sign/scale sweep | Exact W11 close or a predeclared matched-work gain over both frozen O1C-0055/O1C-0056; localization telemetry alone cannot pass | Untested successor only. O1C-0056 proves the address exists, not that any utility-conditioned rule helps; a productive conflict may deserve nonnegative credit |
| `H-MULTIBLOCK-PARENT-CRITICALITY-067` | `SUPPORTED` | 0 | Additional same-key public ChaCha20 blocks compound the unchanged transferred parent-criticality score into stronger complete-key rank on a fresh Full-256 target | O1C-0057 freezes one shared 4,096-decoy panel and scores prefixes 1/2/4/8 before one reveal with unchanged primary/key-rotated/clause-rotated readers | Strict primary improvement over prefix 1, prefix-8 threshold and strict rotation margin under the frozen gate | Achieved: truth ranks 8/7/1/1 of 4,097; prefix-8 rotations 3581/4037; z +5.57888245. This supports a scorer/orderer only; next integrate partial assignments/search, not a larger supplied panel |
| `H-MULTIBLOCK-LOCAL-GRADIENT-068` | `REFUTED` | 0 | The supplied-panel-best eight-block decoy has truth-aligned locally improving one-bit score directions, so signed finite differences accumulated in a 256-cell vault reveal key-bit corrections | O1C-0058 selects one base from 4,096 decoys, scores all 256 one-bit neighbors per block in primary and two rotated controls, freezes prefix-1/2/4/8 syntheses, then reveals once | Primary prefix-8 top-eight confidence bits all correct and a longer correct confidence prefix than both controls, or at least 144 correct bits with +8 over base and strict control margin | Base and primary prefix 8 are both 127/256, improvement 0 and longest confidence prefix 0; controls are 127/128 and every candidate matches 0/8 blocks. Close only attended-best-decoy plus positive one-bit-delta vault; crowd consensus is outside this formal run |
| `H-APPLE-CROSSBLOCK-CONSEQUENCE-069` | `SUPPORTED` | 0 | Making logically redundant feed-forward-cancelled key-lane consequences explicit inside the eight-block shared-key CNF improves propagation and pruning without adding information | Completed matched APPLE-VIEW-0008 comparison against O1C-0061 at identical target, potential, threshold and requested 512/billed 513 conflicts | Achieved: minimum upper bound `24.7944466611→13.1979307788`, safe trail cuts `0→6`, decisions `9166→4471`, propagations `1227877→1178185`; no truth read or key | Preserve the exact clauses and solution set. O1C-0074 now retains 550 exact exclusions behind a K256 live projection; change only bounded attention after its exact fixed point, not consequences, rank or budget |
| `H-EXACT-GROUPED-BOUND-070` | `SUPPORTED` | 0 | Deterministic score-aware compatibility groups retain enough joint factor geometry to make an exact admissible bound both tighter and smaller than frozen pair relaxation | APPLE-VIEW-0009 constructs public groups at widths 6/7/8 and validates exact outward sums plus exhaustive synthetic partial/completion safety | Achieved at width 6: root upper bound `269.7472723039718→262.68644197084643`, groups `3805→2885`, rows `265256→176912`, indexed bytes `2510008→1710776` | O1C-0065 completes native integration with retained cuts, and O1C-0066 carries them across episodes. Preserve the frozen grouping; do not repeat either standalone test |
| `H-EPISODIC-VAULT-071` | `SUPPORTED` | 0 | Canonical threshold-certified no-goods from one fresh bounded APPLE8 solver episode remain useful when preloaded into the next identically bound episode without retaining solver-local state | O1C-0066..0073 establish storage/read/release operators; O1C-0074 executes four bounded complete-attic/K256 episodes with durable reprojection | Achieved: O1C-0074 turns six duplicate recurrence events into a changed active projection, then adds 37 globally novel exclusions one episode later while live residency remains 256 | O1C-0075 closes pure coverage rotation after two byte-distinct pages reproduce the fixed point. Preserve the attic and change relation/operator, not residency alone |
| `H-COMPLEMENTARY-PHASE-072` | `SUPPORTED` | 0 | The sealed vault's fixed point is reader-specific, and a complementary native phase reader reaches distinct exact threshold exclusions at matched bounded work | O1C-0068 reuses the sealed 12-clause vault once with `forcephase=true`, `phase=0`, holding target, CNF, potential, grouping, threshold, seed and 512-conflict accounting fixed | Achieved: 195 fully emitted clauses comprise 190 novel and five duplicate exact exclusions with zero pending; the vault grows `12→202` clauses / `35,061→599,728` literals / `140,483→2,399,911 B` | Mechanism hit, not recovery: status remains unresolved with no model, key or truth. Close phase testing; do not replay lineage ordinal 4, continue phase 0, sweep phase settings or raise the horizon blindly |
| `H-ALTERNATING-READER-COMPOSITION-073` | `REFUTED` | 0 | Exact no-goods discovered by forced phase 0 perturb the complementary forced-phase-1 trajectory enough to expose another distinct exclusion population when both readers share one bounded vault | O1C-0069 imports all 202 sealed clauses and makes exactly one `forcephase=true`, `phase=1`, seed-0, 512-conflict call after every target-free gate passes | At least one novel exact threshold no-good establishes composition gain; a publicly verified candidate is terminal recovery. Search-work telemetry is secondary and duplicate-only cannot pass | Gate fails exactly: one input duplicate, 0 novel, unchanged 202-clause vault. Despite 190 extra phase-0 clauses, conflicts/decisions/propagations/bounds/emission/terminal assignment and native trace exactly equal O1C-0067. Close only passive one-step composition; next require vault-conditioned active steering, not another alternation or sweep |
| `H-VAULT-CONDITIONED-PHASE-074` | `REFUTED` | 0 | The signed cut-literal majority of O1C-0068's 190 novel phase-0 exclusions is a bounded 256-coordinate reader that, when applied as per-variable decision polarity, makes the stored evidence active and escapes O1C-0069's passive phase-1 fixed point | O1C-0070 derives and binds the frozen `139/116/1` field, proves its target-free phase consequence, then makes exactly one local-0/lineage-6 seed-0/512-conflict Full-256 call | A publicly verified key is terminal recovery; otherwise at least one novel exact threshold no-good plus a trace distinct from O1C-0069 establishes active-reader gain. Zero novelty cannot pass | Trace changes to `5c5fb773…`; decisions/propagations fall to `2,297/1,169,826` and minimum UB rises to `18.846601115977638`, so the reader is active. It emits 0/0 novel/duplicate clauses and no key: refute phase-only gain, retain active-not-inert evidence, and do not replay/sweep/scale |
| `H-CONFIDENCE-RANKED-DECIDE-075` | `REFUTED` | 0 | The sealed cut field becomes gainful when its confidence magnitude controls key-variable order through `cb_decide`, while its sign supplies polarity | O1C-0071 freezes one deterministic 255-variable confidence ranking and makes exactly one local-0/lineage-7 call with no phase calls or rank sweep | A publicly verified key is terminal recovery; otherwise at least one novel exact no-good plus distinct active decision telemetry. Zero novelty cannot pass | Active but no gain: 763 calls/499 nonzero/264 zero, 255 unique and 244 redecisions; 0 clauses/model. Tail ranks 249..255 take 1/3/7/15/31/62/125 extras, driving 91,260,183 propagations. Close static same-sign reassertion and do not rerun/sweep |
| `H-BACKTRACK-RELEASE-076` | `SUPPORTED` | 0 | Confidence-ranked evidence is useful only as a one-shot causal intervention: once an injected bit is backtracked, permanently delegating that bit prevents O1C-0071's binary-counter reassertion furnace while retaining early order information | O1C-0072 freezes the monotone consume-once state machine, proves its target-free sequence consequence and makes exactly one local-0/lineage-8 Full-256 call | Achieved at mechanism/work level: 255 once-returns, 255 guided releases, 900 zero fallbacks, 0 redecisions and propagations `91,260,183→5,763,035` (`15.8354x` reduction) | O1C-0073 preserves the primitive and proves it composes with a hard-opposite release stage that emits 311 novel exact clauses. Preserve both; neither result is recovery, entropy reduction or global exhaustion |
| `H-RELEASE-CONTRAST-077` | `SUPPORTED` | 0 | After every confidence-ranked original literal has been genuinely released, returning its currently unassigned hard opposite once exposes a distinct exact threshold-exclusion population without reintroducing same-sign redecision | O1C-0073 freezes the original-first release-gated contrast queue and makes exactly one local-0/lineage-9 call with zero phase/retry/sweep | Achieved at exact-emission level: 255 original/contrast pairs, zero same-sign redecisions and 313 fully emitted clauses containing 311 independently certified novel exact exclusions | O1C-0074 preserves the complete population and adds 37 more exact exclusions behind bounded residency. Preserve the reader; do not replay it under the now-static final projection |
| `H-CAUSAL-ATTIC-078` | `SUPPORTED` | 0 | Separating the complete immutable corpus of certified exact no-goods into an external causal attic from a smaller deterministic active solver reservoir preserves accumulated exclusions while preventing a fixed clause cap from terminating new evidence collection | O1C-0074 freezes an exact/subsumption/occurrence-aware K256 projection and executes four durable 128-conflict episodes with a separate immutable rank source | Achieved: active residency remains 256; complete attic grows `513→550`; six global duplicate occurrences change the active projection and the next episode adds 37 globally novel exact exclusions without capacity termination | O1C-0075 closes pure rotation and O1C-0076 closes parent-zero-only frontier activation. Preserve the attic and advance to `H-RESIDUAL-POLARITY-STAGING-081`; do not replay/sweep K, rank, phase, horizon or RAM |
| `H-NONREPEATING-ATTENTION-079` | `REFUTED` | 0 | After a bounded causal-attic projection reaches an exact fixed point, a deterministic target-free residency rule that prevents immediate state repetition can expose a different useful clause population without enlarging live state or discarding the complete attic | O1C-0075 freezes two fresh K256 pages whose union with the parent covers all 545 undominated clauses, then calls lineages 14/15 once each at 128 conflicts | A changed K256 state after the frozen fixed point yields new exact exclusions, formal frontier progress or a publicly verified candidate; unchanged/repeated state cannot pass | Gate fails exactly: both byte-distinct pages reproduce O1C-0074 episodes 2/3 at trace `f64441a2…`, 2,288 decisions, 2,890,144 propagations and zero prunes/emissions/model. Preserve zero-debt coverage and close pure rotation without a sweep |
| `H-CAUSAL-FRONTIER-080` | `REFUTED` | 0 | The public terminal assignment exposes a nearest resident exact no-good whose 29 unassigned coordinates form a causal boundary; injecting falsifying residual decisions only when the existing reader delegates, then one opposite after genuine release, changes the fixed-point trajectory and exposes new exact evidence | O1C-0076 binds union clause 526 target-free to Page 3 and makes one lineage-16/128-conflict call with the unchanged attic, rank source and resource envelope | A changed trace is the activation gate; science passes only with at least one new exact exclusion, safe prune/frontier contraction, formal exhaustion or publicly verified candidate | Gate fails before science: the first parent zero is callback 256, when all 29 rows are already assigned (18 falsifying, 11 rescue). Cursor 29, zero substitutions/releases/contrasts, unchanged `f64441a2…` trace and zero prunes/emissions/model. Close parent-zero-only activation without replay |
| `H-RESIDUAL-POLARITY-STAGING-081` | `SUPPORTED` | 0 | The two rescue-signed decisions among the five immutable rank/residual intersections are the only public upstream controls before the 24-variable propagation closure; staging their falsifying signs first can redirect the later rescue assignments | O1C-0077 freezes source/effective signs at rank indices 224/226, preserves all rank positions and constructs the existing release-contrast reader from the effective order for one fresh-Page-4 call | Achieved at mechanism level: four staged original/contrast returns and trace change; science still requires a novel exact exclusion, safe prune/frontier contraction, formal exhaustion or publicly verified candidate | Activated without science gain: decisions 2,288→884 and minimum UB moves toward tau, but zero prune/emission/model. Close lineage 17 and advance once to the sealed 11-row prefix |
| `H-RESCUE-PREFIX-PREEMPTION-082` | `SUPPORTED` | 0 | The nine residual rescues that propagation creates before parent fallback can be made causally actionable only by placing the complete 11-row falsifying rescue set ahead of the inherited rank | O1C-0079 executes the unchanged prefix once on fresh Page 6 after the ownership gate | Mechanism achieved: 11 rows consumed first, nine bound/released, two preassigned falsifying, zero rescue skips and changed trace. Science would require prune/clause/closure/model | Supported only at mechanism level; minimum UB remains 4.136043868887843 above tau with zero science outputs. Close prefix activation without retry/refit and move to exact child-bound crossing |
| `H-READER-OWNERSHIP-083` | `SUPPORTED` | 0 | Nested once-only readers compose safely only when proposal history is separated from signed ownership of the currently live decision instance | O1C-0079 combines pure/public fixtures with one Page-6 production call under a typed level-bound owner | Achieved: 549 proposals=bindings=releases, 547 confirmed, two unobserved, zero live/omitted; later `+108/+112` are foreign token 0 rather than owners of retired `-108/-112` | Operational support only, not cryptanalytic gain. Never replay Page 6/lineage 19 or tune ownership; carry the primitive unchanged |
| `H-ONEBIT-BOUND-CROSSING-084` | `REFUTED` | 0 | Exact same-parent evaluation of one candidate bit can expose a safe threshold crossing that whole-trail steering misses, by proving one child below `tau` or closing both children | O1C-0080 consumes fresh Page 7 / lineage 20 once and evaluates every eligible same-parent candidate at exact `128/128/128` work | Activation requires `min(U0,U1)<tau` plus an asymmetric crossing or both children below. Science requires an actual safe prune, certified closure, novel clause or verified model | All 285,725 probes are `NEITHER_PRUNABLE`; minimum child UB `18.464862193097684` remains `3.8586833952047215` above tau. Close Page 7, hard one-bit intervention and depth 2 without retry/sweep |
| `H-BOUND-DIFFERENTIAL-085` | `SUPPORTED` | 0 | Exact child-bound differences contain reusable coordinate-conditioned causal structure after parent-level common mode is removed, even when neither child crosses the hard threshold | O1C-0081 deterministically analyzes only O1C-0080's sealed 16,384 recorded events: within-parent median center and robust scale `d=U0-U1`, stream O(256) moments/sign stability/surprise, and compare permutation/temporal controls without solver or truth | Achieved at mechanism level: after balanced centering, persistent var185 priority is 91.7528 vs permuted max 3.0907; temporal mean correlation 0.8538/sign agreement 81.11%; packed state 28,672 B | Preserve >=37-parent persistence and withhold bit orientation. Convert priority to one target-free action objective in O1C-0082; this is not entropy/recovery/science gain |
| `H-LIVE-CENTERED-PROOF-MINING-086` | `SUPPORTED` | 0 | A live parent-centered differential state becomes search-actionable when residual magnitude selects the coordinate and the current lower-UB child supplies a failure-first proof-mining action without being interpreted as the hidden key bit | O1C-0082 reproduces the state online, composes typed one-shot ownership and consumes fresh Page 8 / lineage 21 once at 128 requested conflicts | Achieved: 255 confirmed actions, 257 strict trail-threshold prunes and 257 globally novel exact no-goods after 9 conflicts; zero-call audit finds the 256-orientation tail cube but no non-tautological simple resolvent and common-core `U=18.66656376905567>tau`; final bank remains 24,576 B | Support is exact-exclusion gain, not key polarity, prefix closure, key recovery, tail-free compression or isolated causal effect. Preserve the clauses in the existing causal attic and never replay Page 8 |
| `H-PARENT-CENTERED-ATTIC-ROLLOVER-087` | `SUPPORTED` | 0 | The O1C-0082 exclusion burst can compound instead of terminating when its complete 257-clause harvest is stored in the external causal attic and only one bounded headroom-aware projection remains solver-resident | O1C-0083 zero-call ingestion proves exact clause/occurrence/union identity and seals explicit `next_active_limit=255` before any fresh lineage-22 call | Achieved at preparation level: all 257 clauses are new unique occurrences; attic reaches 13 chunks / 807 unique clauses / 815 occurrences / 9 strict relations / 801 undominated; fresh Page 9 was 255 clauses / 721,187 literals / 2,885,959 B with `4+43+208`, SHA `8c3b8cc3…`, and 257-clause headroom | Enabling/mechanism gain only, not cryptanalytic/key/entropy gain. O1C-0084 burns Page 9 before native `main`; O1C-0085 then recovers launchability on fresh Page 10 and supplies the next 23-clause rollover input |
| `H-PARENT-CENTERED-COMPOUNDING-088` | `REFUTED` | 0 | The sealed O1C-0082 priority state and Page-9 causal projection can compound the first exact-exclusion burst under a live-continuation-capable runner | O1C-0084 freezes one Page-9 / lineage-22 call after full input preflight | Further globally novel clauses, certified closure/model/key, or attacker-valid entropy/domain gain without exceeding the active caps | Refuted operationally at build transport only: `-Wl,-no_uuid` removes a Darwin-required `LC_UUID`, so `dyld` stops before native `main` or solver construction. The cryptanalytic proposition is untested; Page 9 is burned and must never be retried |
| `H-PARENT-CENTERED-PAGE10-COMPOUNDING-089` | `SUPPORTED` | 0 | A fresh Page-10 projection from the unchanged 807-clause attic can continue the unchanged `05b8acf3…` live bank once build identity is separated from launchability | O1C-0085 builds once with normal Darwin UUID, seals the observed executable, passes `--help` on those exact bytes and consumes one fresh Page-10 / lineage-23 call | Achieved: `23` fully emitted trail-UB no-goods are all active-page new and globally novel against the 807-clause attic; next vault is available and the bank evolves to `2c0c4ccb…` | Support is exact-clause compounding only: `actual_certified_prunes=0`, no key/model/closure/entropy/domain gain. Page 10 is burned; roll all clauses and the evolved bank into fresh Page 11 |
| `H-PARENT-CENTERED-PAGE11-ROLLOVER-COMPOUNDING-090` | `SUPPORTED` | 0 | The 23-clause O1C-0085 gain and evolved `2c0c4ccb…` bank can be atomically rolled into the causal attic and a fresh bounded Page-11 projection, then continue exact-exclusion compounding without replay or cap growth | O1C-0086 seals the 830-clause baseline, Page 11 and live bank, then consumes one fresh lineage-24 call | Achieved: all 23 inputs are preserved and the production call emits 202 distinct clauses / 546,864 literals with zero intersection against the 830-clause attic; next vault remains available and bank evolves to `658fd285…` | Support is exact-clause compounding only: `actual_certified_prunes=0`, no key/model/closure/entropy/domain gain. Page 11 is burned; roll all 202 clauses and the evolved bank into fresh Page 12 |
| `H-PARENT-CENTERED-PAGE12-ROLLOVER-COMPOUNDING-091` | `SUPPORTED` | 0 | O1C-0086's 202-clause burst and evolved `658fd285…` bank can be retained in the immutable attic and projected into fresh bounded Page 12 without losing the compounding mechanism | O1C-0087 preserves all 202 clauses on bounded Page 12; O1C-0088 then consumes fresh lineage 25 once with the unchanged operator | Achieved: the production call emits 259 distinct clauses / 744,973 literals, all globally novel against the complete 1,032-clause attic, in only 55 billed conflicts; bank evolves to `0203de9f…` | Support is exact-clause compounding only: `actual_certified_prunes=0`, no key/model/closure/entropy-domain gain. Page 12 is burned; preserve all 259 clauses and the evolved bank through zero-call Page 13 rollover |
| `H-PARENT-CENTERED-PAGE13-ROLLOVER-COMPOUNDING-092` | `SUPPORTED` | 0 | O1C-0088's 259-clause burst and evolved `0203de9f…` bank can be retained in the immutable attic while the exact one-slot capacity overflow is removed by a fresh Page-13 projection at active limit 253, then continue exclusion compounding | O1C-0089 preserves all evidence on bounded Page 13; O1C-0090 consumes fresh lineage 26 once with the unchanged operator | Achieved: 260 distinct clauses / 743,794 literals are globally novel against the complete 1,291-clause attic at only 46 billed conflicts; bank evolves to `715bfbc2…` | Support is exact-clause compounding only: `actual_certified_prunes=0`, no key/model/closure/entropy-domain gain. Page 13 is burned; preserve all 260 clauses and evolved bank through zero-call Page 14 rollover |
| `H-PARENT-CENTERED-PAGE14-ROLLOVER-COMPOUNDING-093` | `SUPPORTED` | 0 | O1C-0090's 260-clause burst and evolved `715bfbc2…` bank can be retained in the immutable attic while another exact one-slot sacrifice creates fresh Page 14 with 260-clause headroom, then continue exclusion compounding | O1C-0091 preserves all evidence on bounded Page 14; O1C-0092 consumes fresh lineage 27 once with the unchanged operator | Achieved: 261 distinct clauses / 756,414 literals are globally novel against the complete 1,551-clause attic at only 10 billed conflicts; bank evolves to `97a325c9…` | Support is exact-clause compounding only: `actual_certified_prunes=0`, no key/model/closure/entropy-domain gain. Page 14 is burned; preserve all 261 clauses and evolved bank through zero-call Page 15 rollover |
| `H-PARENT-CENTERED-PAGE15-ROLLOVER-COMPOUNDING-094` | `REFUTED` | 0 | O1C-0092's 261-clause burst and evolved `97a325c9…` bank can be retained in the immutable attic while one more live slot creates fresh Page 15 with 261-clause headroom and a valid continuation call | O1C-0093 preserves all 261 clauses and seals Page 15; O1C-0095 then consumes lineage 28 once | A persisted science-bearing result from the unchanged Page-15 operator after exact preparation | Refuted operationally at adapter transport only: native v26 completes and returns JSON, but adapter v29 omits two native receipt-provenance fields and rejects before stdout/result persistence. The cryptanalytic proposition is untested; Page 15 is burned and must never be retried |
| `H-PAGE14-NINE-AXIS-QUOTIENT-095` | `SUPPORTED` | 0 | O1C-0092's 261-clause family has an exact lossless shared-core plus nine-axis repetition quotient suitable for bounded O1/holographic state | O1C-0094 reconstructs all canonical clauses and witness identities from the quotient and proves exact aggregate equality without target/truth or solver calls | Achieved: exact 261-row clause/witness round trip, `15.9198x` conservative literal-entry compression, 18,034 B packed retained state and 29,766 B maximum streaming decoder | Compression is not a key consequence. The cheap 218-polarity score-bound screen certifies 0/109 nontrivial relations; do not spend a Page on equivalence proof without a stronger local certificate |
| `H-PARENT-CENTERED-PAGE16-CONTRACT-RECOVERY-096` | `SUPPORTED` | 0 | The unchanged 1,812-clause attic and `97a325c9…` bank can enter fresh Page 16 / lineage 29 after repairing the exact native-output/adapter schema contract, allowing the unchanged compounding proposition to receive a retained science result | O1C-0096 completes zero-call Page-16 derivation; O1C-0097 composes actual native output through adapter v30 and consumes one fresh lineage-29 call | Achieved: the repaired transport persists a complete `128/21/21` result with 262 globally novel clauses against the 1,812-clause baseline; bank evolves to `8100bccf…` | Support is exact-clause/transport gain only: 255 actions have zero certified crossings and there is no key/model/closure/entropy-domain gain. Page 16 is burned. Audit closes 263 occurrences as 262 unique global-new clauses, with indices 6/7 the sole duplicate. O1C-0098 subsequently appends all occurrences as one exact 262-clause / 745,152-literal / 2,981,847-byte chunk and seals the 2,074-clause attic; fresh Page-17 continuation now belongs to H097 |
| `H-PARENT-CENTERED-PAGE17-ROLLOVER-COMPOUNDING-097` | `INCONCLUSIVE` | 0 | O1C-0097's complete 263-occurrence harvest and evolved `8100bccf…` bank can be retained in the immutable attic while active limit 249 creates fresh Page 17 with enough clause headroom for a duplicate-free 263-clause successor, then continue unchanged exact-exclusion compounding | O1C-0098 completes the zero-call rollover; O1C-0099 consumes Page 17 / lineage 30 once with the unchanged operator | O1C-0098 achieves the exact 19-chunk / 2,074-clause preparation and `249+263=512` capacity. The science proposition requires a retained O1C-0099 result | O1C-0099 hits the fixed 65,536 ownership-event telemetry cap before returning any native result. It is neither support nor scientific refutation. Page 17 is burned; import no output and transfer the still-untested proposition to a fresh-page bounded-telemetry successor |
| `H-PARENT-CENTERED-PAGE18-BOUNDED-TELEMETRY-100` | `SUPPORTED` | 0 | The unchanged O1C-0098 attic and `8100bccf…` bank can enter fresh Page 18 while exact owned-token lifecycle plus a streamed digest of non-claiming assignments removes the telemetry terminal without changing the parent-centered scientific operator | O1C-0100 zero-call reprojects the certified state to Page 18 / lineage 31 and imports only O1C-0099's failure receipt. O1C-0101 versions native producer and adapter together, proves >65,536 non-claim observations stay bounded, then consumes the fresh lineage once after one focused gate and preflight | Achieved: O1C-0101 completes 72,656 ownership events with only 774 lifecycle rows retained, returns status 0 at `128/36/36`, and emits 264 distinct globally novel clauses / 766,686 literals against the 2,074-clause attic; bank evolves to `a8e137b1…` | Support is exact-clause and bounded-streaming transport gain only. No key/model/native closure/entropy-domain gain; Page 18 is burned. Preserve the 264 true occurrences and keep the five post-result derived resolvents in a separate certified namespace |
| `H-PAGE19-CERTIFIED-RESOLUTION-OVERLAY-102` | `SUPPORTED` | 0 | The exact five-node single-pivot closure inside O1C-0101's native harvest can be certified without target/truth, kept separate from native occurrences, and its three-clause undominated antichain can replace dominated/low-priority residents on fresh Page 19 while retaining 264-clause headroom | O1C-0102 atomically seals the emitted/derived namespaces and Page 19; O1C-0103 consumes lineage 32 once with the unchanged bounded operator and the full 2,343-identity novelty registry | Achieved at the exact-exclusion/closure-guided compounding level: O1C-0103 retains 265 globally novel native clauses and its zero-call fixed point adds 84 derived clauses, with a 52-clause undominated overlay that strictly subsumes 65 new native clauses | Support is not key recovery or attacker-valid entropy/domain reduction. Page 19 is burned. Never invent a native witness/occurrence for a resolvent or conflate the 84 proof identities with native emissions |
| `H-PAGE20-CLOSURE-COMPOUNDING-104` | `INCONCLUSIVE` | 0 | O1C-0103's 265-clause native burst and 84-clause exact proof closure can be retained losslessly in separate namespaces while a 52-clause derived antichain strengthens the then-fresh Page 20 without sacrificing the observed 265-clause successor capacity | O1C-0104 completed the atomic zero-call rollover at active limit 247; O1C-0105 consumed Page 20 / lineage 33 once after one focused contract gate and preflight | Preparation was achieved exactly: 21 chunks / 2,603 native, five inherited derived + 84 new derived, append-only registry 2,692, Page `537f63c5…` with 265 slots and manifest `2e784eea…`. O1C-0105 terminated in adapter validation before command/native launch because 11 valid resolvents are not standalone score-certified; actual/billed are null and no scientific outcome exists | Operationally terminal and scientifically inconclusive, not a cryptanalytic negative. Page 20 is burned. Preserve both proof namespaces and transfer the untested compounding proposition only through H106's typed active-vault boundary |
| `H-PAGE21-TYPED-ACTIVE-VAULT-106` | `SUPPORTED` | 0 | Separating standalone score-certified active clauses from general resolution-certified sidecar memory lets the retained 2,692-identity state continue on a fresh page without discarding proof evidence or passing the wrong clause type to native | O1C-0106 sealed Page 21 / lineage 34 with 203 emitted + 3 inherited-derived + 41 new-derived ACTIVE residents and eleven pivot-223 consequences sidecar-only; O1C-0107 bound and re-certified the exact 247 rows before one call | Achieved live: O1C-0107 completes at `128/57/57`, emits 266/266 globally novel native clauses / 752,466 literals and evolves bank `c0db45c1…→62360d82…`; the typed boundary admits a full retained result | Support is exact-clause/type-safe compounding, not key recovery or entropy/domain reduction. Page 21 is burned. Preserve every proof and continue only through H108's fresh typed projection |
| `H-PAGE22-TYPED-CLOSURE-COMPOUNDING-108` | `SUPPORTED` | 0 | O1C-0107's 266-clause native harvest and denser 153-clause pivot-32 fixed point can be retained losslessly while only score-certified proof consequences enter a fresh bounded Page 22 | O1C-0108 appends all native occurrences, preserves all 153 resolvents, independently classifies 149 passing / four sidecar-only, carries bank `62360d82…`, and seals Page 22 / lineage 35 at active limit 246 before any new call | Achieved: 22-chunk attic, 3,111-identity registry, 246/246 certified Page `18387804…`, exact 266-clause headroom and manifest `f16d505f…` | Preparation support only, not key recovery. Never relabel derived clauses as native, activate failed indices `1,2,32,55`, flatten proof provenance, or replay Page 21 |
| `H-PAGE22-BOUNDED-BREADCRUMB-COMPOUNDING-109` | `SUPPORTED_MECHANISM`; direct sign `REFUTED` | 1 | A behavior-preserving fixed sidecar can retain local one-/two-sided threshold structure discarded by the zero action-crossing counter while the typed Page-22 state still compounds into a third globally novel Full-256 harvest | O1C-0109 binds exact Page/registry/bank seals, retains at most 256 canonical breadcrumb rows before mutation, and consumes lineage 35 once; O1C-0111 freezes the relative-bound sign reader before any truth access | Compounding achieved: 267/267 globally novel clauses and bank growth by 33,569 probes; 100/100 breadcrumbs expose 37 ONE + 63 BOTH events over coordinates 193/196. Direct sign then scores `0/2` in both arms | Preserve the event field for causal selection but close its raw sign as key orientation. Never call 100 rows 100 bits, flip after reveal, retry Page 22, or infer posterior/entropy gain from clause harvest |
| `H-PAGE23-TYPED-CLOSURE-COMPOUNDING-110` | `SUPPORTED` | 0 | O1C-0109's 267-clause harvest and exact 48-clause pivot-194 fixed point can be retained losslessly while every active row remains a standalone score-certified no-good on a fresh bounded page | O1C-0110 appends the native chunk, derives the fixed point, v8-certifies all 48 new consequences, and atomically publishes Page 23 without a solver/reveal/truth call | Achieved: attic 23 chunks / 3,136 unique, logical registry 3,426, Page 23 exactly 245/245 certified with 267 clause slots; manifest `f1b9bd7f…` | Enabling/state support only. Keep Page 23 fresh until the next frozen operator and irreversible-call contract are explicit; no key/posterior/entropy claim follows |
| `H-BREADCRUMB-DIRECT-SIGN-111` | `REFUTED` | 0 | The sign of the hidden ONE/BOTH local-prunable field directly orients the corresponding secret key bit | O1C-0111 freezes both readers before one broker-verified historical truth check | Both arms are `0/2`; identity loses to global sign flip and ranks `256/256` | Do not invert or rescale after reveal. Retain event identity only and require an independently learned output-conditioned orientation |
| `H-FULLBANK-MARGINAL-SIGN-112` | `REFUTED` | 0 | An unconditional coordinate-wise marginal of the live 24,576-byte bank is already a 256-bit key posterior | O1C-0112 freezes primary `-final.robust_z_mean` plus six diagnostics before one historical truth check | Primary `130/255`, tail `0.401134`, rank `198/256`, negative sign-flip margin, zero exact bytes/words; no secondary gate passes | Close sign/scale/normalization/offset sweeps on this consumed bank. Preserve it as causal/query-priority state and learn conditioned relations across BUILD targets |
| `H-PAGE22-LIVE-COMPOUNDING-109` | `ACTIVE` | 0 | The proof-dense typed Page 22 and evolved `62360d82…` bank can compound a third retained Full-256 gain while a bounded local-prunable breadcrumb sidecar exposes the first bridge from consumed-coordinate probe geometry toward a posterior | Version producer/adapter/runner together, preserve at most the locally prunable probe rows without changing decisions, certify exact Page 22 before intent, then consume lineage 35 once | Further globally novel clauses or a retained, attacker-computable signed breadcrumb that can be scored as key/posterior/search evidence | Clause gain and key evidence remain separate metrics. Never replay Page 21/22, reveal truth before freeze, or let telemetry change action behavior |
| `H-PROOF-RELATION-041` | `SUPPORTED` | 0 | H16 branch-difference clause multisets contain portable target-specific signed key-to-internal relations without target-key input | O1C-0039 froze H16/`|J|=0.5` on four BUILD keys, then opened two DEVELOPMENT labels only after both bounded fields and search arms froze | Above-chance primary relation accuracy on both targets and a pooled margin over key- and factor-rotated endpoint controls | Achieved: 55.09%/56.99%, pooled 55.84% versus 52.88%/49.51%; preserve exact field and test aggregate candidate rank rather than resweeping extraction |
| `H-FACTOR-SEARCH-042` | `REFUTED` | 0 | Aggregating the transferred relation field over a complete attacker-computable forward execution ranks the true key and can become reversible live CDCL guidance | O1C-0040 scores 4,096 deterministic decoys per consumed DEVELOPMENT target with raw and one frozen structural-surprise objective | Positive true-key rank on both targets plus geometric margin over endpoint rotations | Raw ranks 1905/4097 and 2292/4097; surprise 1078/4097 and 1461/4097, dominated by key rotation 107/4097 and 423/4097. Close occurrence scoring |
| `H-ANTECEDENT-043` | `SUPPORTED` | 0 | Branch-exclusive signed proof-antecedent chains retain target-dependent causal path identity that terminal clause occurrence collapses into universal ChaCha geometry | O1C-0043 consumed ranks 5/91/141 and O1C-0044 fresh rank 54/4097 with both rotations below truth | Fresh primary rank below 0.25 and strict margin over both endpoint rotations | Achieved; next test consequence in matched exact search rather than another representation panel |
| `H-CHAIN-LEAF-044` | `REFUTED` | 0 | A global `-1` weighted sum over unique signed leaves of branch-exclusive H16 proof chains transfers complete-key rank concentration | O1C-0042 froze the exact O1C-0041 extractor and scorer before one sealed target | Fresh primary rank below 0.25 and above both rotations | Primary 1371/4097 keeps a small control margin but fails 0.25; close without retry and preserve parent role/criticality |
| `H-PARENT-CRITICALITY-045` | `SUPPORTED` | 0 | A bounded BUILD-fitted reader over ordered RUP parent role, original functional-clause criticality, expected pivot and unique-literal phase transfers complete-key joint rank | O1C-0044 freezes exact O1C-0043 reader before one sealed target | One sealed fresh rank below 0.25 and strict margin over key/clause rotations | Achieved at 54/4097 versus rotations 3567/2972; preserve exact reader for live-search consequence |
| `H-KEYGUIDE-039` | `REFUTED` | 0 | A frozen O1 unary field reduces exact full-round search when supplied as confidence-ordered reversible key decisions | O1C-0037 compared internal, phase, real O1, coordinate-shuffled and exact/one-error ceilings on consumed `build-0000` | Less matched search work than both internal and shuffled, or repair of one wrong hint within 32,768 conflicts | Real O1 K256 is 2.123x internal wall and telemetry-identical to shuffled; one wrong hint is unresolved through 32,768 conflicts. Close key-phase-only coupling and move decisions inside relation/proof structure |
| `H-RESIDUAL-040` | `SUPPORTED` | 0 | The unchanged exact ChaCha20-R20 relation can complete a nonzero O1-ordered residual once every supplied prefix decision is correct | O1C-0038 used exact post-reveal signs and frozen O1 confidence order at residual widths 0/1/2/4/8/9/16 | Exact public verification at a nonzero residual width under a bounded conflict budget | Width 8 recovers in 89 conflicts / 135,441 us; width 9 remains unresolved through 32,768 conflicts. Retain this as a decoder ceiling, never as attacker-valid recovered bits |
| `H-TRANSFER-036` | `REFUTED` | 1 | A448's frozen proof-antecedent top4 reader plus exact A442 Borda backbone retains better-than-median byte-3 ordering when the other 248 key bits are unknown | Completed O1C-0031 on consumed RFC8439, then O1C-0032 unchanged on disjoint consumed DEVELOPMENT-0000 | Rank `<=128/256` on both consumed targets, followed only then by one fresh blind target | RFC rank `47` did not repeat: DEVELOPMENT rank `239`; baseline/proof/hybrid were `242/236/239`. Close without spending a fresh target or resweeping any operator, byte, sign or coefficient |
| `H-TRANSFER-034` | `REFUTED` | 1 | A291/A296's frozen shallow conflict/XOR reader retains some byte-value ordering when its exact H1/2/4/8 RawCell field is measured on an all-256 public target with the other 248 key bits unknown | Completed literal byte-2 transfer on RFC plus two consumed DEVELOPMENT targets, then one parameter-unchanged fresh EVALUATION target | Better-than-uniform byte ranks on the consumed panel followed by a positive fresh target | Consumed ranks `118/61/9` failed to generalize: fresh rank `230/256`, four-target rank-product `p=0.1766`. Close without byte/sign/coefficient resweeps; retain only the exact 46–56 s cube adapter |
| `H-RESIDUAL-035` | `ACTIVE` | 0 | The sibling A526/W52 recovery engine can close a full256 O1 posterior once its native 204-bit complement is exact or appears in a tractably small beam | O1C-0035 now applies an exact top-K decoder only to coordinates 52..255 and emits the existing A526 handoff without changing the backend | A526 requires `204/204`, or an explicitly bounded completion beam containing an exact member; a backend hit must pass independent public ChaCha verification | Literal bridge is complete. Current O1C-0022 top-65,536 beams contain `0/20` exact complements; MAP max is `118/204`, post-reveal oracle max `123/204`. Keep decoder/backend frozen and change only upstream eight-block public evidence |
| `H-TRANSFER-037` | `REFUTED` | 0 | A465's exact cubic rank-PoE preserves A448's apparent RFC byte advantage after the known complement is removed | Apply A460/A462/A463 and frozen `(7,1,4)` A465 fusion to both retained all256 fields without new solver work | Both consumed truth bytes better than median before any fresh target | A465 leaves ranks at `47/239`; close the exact transfer and do not resweep |
| `H-TRANSFER-038` | `REFUTED` | 0 | A469's exact positive bucket-local interaction rescues the all256 A465 field | Apply the frozen A469 correction to both prediction-frozen A465 fields without labels or refit | Both consumed ranks improve before any fresh target | Ranks become `56/239`; close the exact transfer and do not resweep |
| `H-TRANSFER-039` | `REFUTED` | 0 | Direct terminal assumptions, failed cores, inverse fixed points or local W8 relaxation around an O1 candidate expose a reusable pre-W52 error-bit field | Completed cheap consumed screens and one unchanged repeat where a breadcrumb appeared | Repeated per-bit correction or stable group correlation that reduces complement Hamming error | Cores remain 255..256 bits; inverse ranking is chance; frozen correction loses 14 DEVELOPMENT bits; neighbor correction splits -4/+6; W8 correlation collapses -0.158 to -0.014. Close these exact surfaces and require a genuinely new raw source |
| `H-LIVE-009` | `REFUTED` | 1 | The O1C-0009 linear full-256 direct/candidate contrast readers remove reproducible code length from a uniform unseen ChaCha20 key | Completed on 128 broker-secret keys with all predictions frozen before reveal, then its only signed breadcrumb replicated on 2,048 more | Held-out total NLL stably below 256 bits with matched controls at chance | O1C-0009 froze at zero and O1C-0010 signed direct reached -0.019088 bit; raw end-output regression is closed |
| `H-SIGNED-013` | `REFUTED` | 1 | O1C-0009's CAL-only negative direct scale reflects a tiny transferable orientation rather than panel noise | O1C-0010 froze exact model/scale and evaluated 2,048 new broker-secret keys without refit | Positive mean compression, conditional and target-level evidence, plus matched frozen-control margins | Direct compression was -0.019088 bit, z -0.946, shuffled margin -0.017541 and output-permutation margin +0.000962; do not revisit |
| `H-CNF-015` | `SUPPORTED` | 1 | A target-independent exact symbolic-output ChaCha20 CNF can expose an attacker-valid paired-assumption relation without retaining a target trace or reducing key width | O1C-0011 compiled, double-reproduced and solver-checked the complete one-block relation before reader training | Exact SAT/UNSAT mapping checks, zero target key units, per-bit causal ranges and bounded-memory generation | Achieved: 32,128 vars, 187,370 clauses, 656 x 32 bit ranges; retain immutable template and move event efficacy to H-SOLVER-014 |
| `H-SOLVER-014` | `REFUTED` | 1 | O1C-0019's learned unary paired-assumption packet reader and stationary picker contain portable all256 key orientation | Completed exact four-fold artifact-only BUILD-LOO gate | Positive raw-reader compression over its untrained twin plus positive live-policy compression | Learned policy `-0.271090`; raw learned `+0.312764` loses untrained `+0.371233`. Close this exact unary reader/picker; a future interaction/antecedent source is a new hypothesis, not a rescue fit |
| `H-ORIENT-016` | `REFUTED` | 1 | The O1C-0013 BUILD/CAL procedure identifies a portable global orientation for the O1C-0012 h96 proof-prefix field | Completed exact no-refit sequence across O1C-0013/14/16 | Positive total compression and target robustness on the 32-key panel | O1C-0016 h96 is -0.175 bit/key; pooled exact h96 across 42 keys is -0.08457 with z +0.134; close this orientation |
| `H-POLYPHASE-017` | `REFUTED` | 1 | Equal-logit compounding of h96 and h65 stabilizes the unary inverse direction | O1C-0016 froze exact readers and composition before 32 fresh target entropy calls | Positive ensemble/control margin, at least 18/32 positive, and promotion z >=1.64485 | Ensemble is -0.078249 bit/key, 11/32 positive, paired z -0.555 and promotion z 1.004; do not search weights |
| `H-ONLINE-018` | `SUPPORTED` | 1 | A shared O1 reader can discover an anonymous oriented channel online and an exact bounded 256-coordinate Bit-Vault can retain its addressed readings across a full-width stream without current-target gradient updates | O1C-0017 trained after eight known BUILD freezes, then froze five arms on 16 disjoint synthetic full-256 episodes with zero fresh entropy | Achieved: +42.308742 bits, 80.224609% accuracy, 16/16 positive; +46.701393 over ablation, +42.764897 over shifted labels, +47.231321 over raw O1 end state; every invariant passed | O1C-0020 separately upgrades explicit long-stream retention to exact; real causal orientation and picker agency remain the O1C-0019/O1C-0021 frontier |
| `H-RETENTION-022` | `SUPPORTED` | 1 | A BUILD-trained O1 gate can select 256 public bindings from one unified stream and an exact addressed Bit-Vault can retain them through at least `2^20` distractors without an oracle deployment mask or O(T) model state | O1C-0020 froze 32 BUILD and 8 CAL seeds, then evaluated 4 unseen seeds at H=0/2^16/2^20 with shuffled, untrained, cue, all-open and storage controls | Achieved: 12/12 cells at 256/256, TP=256/FP=FN=0, nested 352-byte state exact, global route margin +0.454628; every longest control fails | Freeze the API; explicit binding values are solved, so move to H-CAUSAL-023 rather than extending the same haystack for vanity |
| `H-CAUSAL-023` | `ACTIVE` | 1 | The same bounded O1 route/vault API can learn reliability-weighted accumulation of weak and contradictory coordinate evidence on unseen 256-bit streams rather than merely store explicit values | O1C-0021 is source-frozen at `4ba1cc6`: after sibling compute clears, repeat the two-seed broad DEV check and then score four never-used 256-bit EVAL episodes once against zero-prior, last, unit-sum, static/current-marker, shuffled, all-open and an independent bounded public-FSM reference | Positive held-out compression in every seed, calibrated evidence growth with stream length, material equal-work control margins and unchanged bounded live state; exact 256/256 is the terminal synthetic gate | If routing works but accumulation fails, change only update/decay/confidence semantics; if routing fails, enrich public event observability before touching real solver streams |
| `H-REAL-VAULT-024` | `REFUTED` | 1 | O1C-0019's frozen incremental q-deltas compound usefully in the exact 352-byte addressed vault as coverage grows from 12 to 256 coordinates | Completed O1C-0022 K=12/52/128/256 replay | Positive K256 compression and exact-complement improvement | Int8 curve falls from `-0.006772` to `-1.181837` bits; raw/normalized/unit-sign K256 are also negative; no backend entry gate passes. Close the packet/vault field without scale, quantizer or reader sweep |
| `H-COMPOSER-025` | `PARKED` | 1 | A complete frozen result can drive O1-O as a deterministic non-repeating experiment composer | O1C-0023 remains source-frozen but unrun | Execute only when a positive evidence field needs autonomous optimization | O1C-0022 is null; composing another operator over it is process work without recovery ROI. Keep the code, spend no run |
| `H-FRONTIER-026` | `SUPPORTED` | 1 | Exact all-coordinate factorized top-K decoding fixes the restricted uncertainty cube's blind spots and gives a reusable full-256 recovery frontier, but useful recovery still requires a concentrated upstream posterior | O1C-0024 exhaustively proved low-width order, recovered a constructed 20-round rank-four key outside the matched local cube, then decoded 65,536 frozen candidates from one burned O1C-0016 posterior | Achieved mechanism: exact deterministic global ordering and public verification; burned diagnostic honestly reports 0 hits, MAP H117 and best H110 at rank 15,405 under all lifecycle/resource gates | Freeze decoder `36133bc`; do not scale K on the opened null. Feed it only a future held-out posterior with portable entropy reduction and add matched search-diversity controls |
| `H-LOGIT-FRONTIER-027` | `SUPPORTED` | 1 | A frozen full-256 natural-logit field can enter the exact global frontier without sigmoid saturation or rounded `/ln2` ordering while remaining bound to its public-target lifecycle | O1C-0025 source `b008e21` covers exact binary64-logit integer ordering, the fixed 57,344 B O1C-0022 tensor to 2,048 B K256 `quantized_int8_vault` slice, the internally verified supplied upstream freeze chain and K=65,536 | Achieved instrument: 14 focused plus 18 neighboring tests pass (32 tests / 80 subtests); 65,536-candidate smoke is 0.937653 s at 44,384,256 B peak | Keep the handoff fixed and unreserved until O1C-0022 supplies positive frozen logits; an instrument pass is not evidence and must not motivate decoder tuning |
| `H-PROOF-ANCESTRY-028` | `PARKED` | 1 | A fixed second-order ancestry-touch x proof-context bias may add portable orientation beyond the frozen O1C-0022 scalar output | Source-frozen proxy v2 `0af57fb` already passes label-free real-FAP structure, exact swap/control and 8-KiB-state tests; only if authoritative finalized O1C-0023 selects `proof_ancestry_pair_residual_v1`, reserve the four-fold residual run on consumed BUILD FAPs | Positive compression and offset improvement in all four folds plus strict margins over all-256 identity shuffle, additive-factorized and common-mode controls | If O1C-0023 selects another operator, do not run it; if selected and null, close only `fap_ancestry_touch_bilinear_proxy_v2`, retain parent R07 and advance to a raw antecedent/signed-pair sensor rather than tuning the decoder |
| `H-PAIR-PROXY-029` | `SUPPORTED` | 1 | A self-preserving 16x8 projection can remain pair-identity-sensitive without giving the factual arm a scale advantage or exceeding bounded deployment state | Completed label-free replay of all four BUILD FAPs with primary and all-256 deranged touch control | Achieved: RMS ratio 1.008770, cosine 0.027591, identical only on 85 branch-empty rows; 1.609594 s, 105,955,328 B process peak; effective reader plus posterior exactly 8,192 B | Structural support is not key orientation; retain the frozen v2 basis and wait for the authoritative residual-label lifecycle |
| `H-HOT-READOUT-030` | `SUPPORTED` | 1 | For a fixed encoder/kernel/phase basis, one T-independent full-256 polyphase state can support multiple late-bound readout weights and temperatures without replay or state mutation | O1C-0027 consumed one deterministic `384x3x256` stream once, queried four immutable readers, compared an independent complex128 recurrence, rechunk/swap controls, a collapsed-bank null and three foreign bases | Achieved: 25,096 B per state; minimum normalized reader RMS 0.081663 versus 1.24e-16 collapsed null; exact rechunk/swap/serialization; zero query reingestion/state writes; every foreign basis rejects | Bind O1-O choices to immutable hot specs and next ingest real O1C-0022 causal packets; any encoder/kernel/phase change remains cold and uses a new attempt |
| `H-HORIZON-HOT-031` | `SUPPORTED` | 1 | A complete O1C-0022-compatible K256 packet ledger can enter one allocation-invariant bounded state in canonical horizon-major order and accept lineage-bound O1-O reader choices as hot parameters without evidence replay | O1C-0028 independently reconstructed the pinned packet ABI, compared normalized/int8 transports and sparse/order controls, then repeated its V2 result in eight fresh processes | Achieved: all 14 gates pass; 25,128 B state, one primary consume, zero primary reingestion, result SHA `ed3517f2...`, 42.8125 MiB peak and 0.143779 s capsule time | Freeze the ABI. After authoritative O1C-0019/22, use O1C-0029 for the real packet stream; O1C-0023 is optional metadata only, while evidence/address/basis changes remain cold |
| `H-STACKED-HOT-032` | `PARKED` | 1 | A hot H64/H65/H96 readout can recover orientation from the real O1C-0022 packet field | O1C-0029 remains source-frozen and unrun | Run only after an upstream field has a positive recovery effect | O1C-0019 learning loses untrained and every O1C-0022 K256 field is negative. A hot readout of the same null evidence is not the next paid experiment |
| `H-DIAGONAL-LAMP-033` | `REFUTED` | 1 | Same-coordinate diagonal self-ancestry innovations become a portable key-orientation lamp when each horizon increment is amplified by its local exact-conflict confidence bit | O1C-0030 completed four BUILD leave-one-out folds on the already-consumed real FAPs with one RMS-standardized global scalar per arm, predictions frozen before each heldout label open, cumulative/legacy/deranged/even controls and exact top-65,536 decoding | Positive primary compression in all four folds, strict improvement over cumulative replacement in all four and a positive margin over fixed deranged confidence | Primary is -0.680620 bit/key, positive in 3/4 but trails cumulative in 4/4 by -0.582832 bit mean; deranged confidence wins at +0.779642 and no exact key enters top 65,536. Close only summarized diagonal self-ancestry plus local hard-q attachment; retain raw antecedent identity, signed interactions, the learned full-330D reader and live global scout-to-focus |
| `H-AGENCY-020` | `REFUTED` | 1 | O1C-0019's target-conditioned preview and learned abstention select more key information than fixed routing | Completed exact four-fold O1C-0019 comparison | Positive final compression and positive margins over hash, shifted and static controls | Learned final mean `-0.271090`; all three IAUC margins are negative; STOP and no-STOP are identical because every fold exhausts the field. Close this picker |
| `H-CARRIER-021` | `REFUTED` | 1 | Separate unary horizon carriers plus incremental gating preserve useful all256 orientation | Completed O1C-0019 and O1C-0022 | Positive learned-reader advantage and positive K256 accumulation | Learned reader loses untrained by `0.058470` bit and all K256 accumulators are negative. Retire global unary packets |
| `H-MOBIUS-019` | `ACTIVE` | 2 | Sparse Boolean Möbius pair interactions cancel common mode and expose carry/proof innovations absent from unary polarity differences | Only after an O1C-0022 all-float null, test projected causally linked signed-variable pairs/proof antecedents on consumed BUILD folds after nuisance centering; compare m_ij with matched balanced rotations at equal work | Cross-key residual orientation and information gain beyond degree-1 under strict held-out folds | If no lift, suppress degree-2 family contextually and retain coverage-debt exploration without exhaustive pair expansion; never fall back to the A539/A541-closed single-position additive marginal |
| `H-CONTRAST-010` | `PARKED` | 3 | Structured one-bit, Gray, repeated-word and posterior-directed candidate traces teach a reusable inverse operator that later transfers to uniform random proposals | O1C-0009 tested the first linear realization | More predictable bits or lower NLL on uniform unseen targets, not only structured keys | Resume only when a solver/carry sensor supplies nonzero local evidence; do not enlarge the same end-output reader |
| `H-TEACHER-011` | `REFUTED` | 3 | O1C-0009's privileged target round/carry auxiliary loss distills an output-only linear inverse without entering deployment inputs | Completed with the same public deployment schema as the relative arm | Distilled arm improves uniform held-out NLL while target-time schema remains public-only | Distilled CAL scale was zero; future teacher labels must supervise an attacker-observable solver event, not raw output alone |
| `H-INTERACT-012` | `PARKED` | 2 | A learned multi-wavelength PoE plus A469 positive bucket-local interaction can improve a stable residual causal posterior | O1C-0018 did not establish a raw unary backbone; O1C-0019 must first correct accumulation and produce a stable packet posterior | Resume only after packetized unary BUILD-LOO lowers NLL; interaction must then improve it without degrading controls | Never place interaction on a null or double-integrated unary field; use identity-preserving residual correction only |
| `H-STAGE3-001` | `ACTIVE` | 1 | Manifest-pinned pre-result solver or trajectory fields contain attacker-computable information that transfers to a held-out target ordering | Build one small frozen dataset and compare factual labels with shuffled/matched controls on disjoint source artifacts | Stable validation lift with identical work and no target-derived inputs | Localize by feature family, then pivot from aggregate metrics to temporal deltas |
| `H-MEM-002` | `SUPPORTED` | 2 | A stream-length-bounded spectral/slot state can retain the useful order of a fixed 4,096-cell Direct12 stream without explicit candidate rows | Compare global sparse Walsh, multi-slot Walsh and quantized Bit-Vault arms under exact state/work accounting | High transfer fidelity with exact state accounting | Achieved mechanistically, but O1C-0006 proves the full-rank bank is table-equivalent and larger than the matched direct table; it is a ceiling, not compression |
| `H-O1O-003` | `ACTIVE` | 2 | Adaptive operator selection gives more validation information per billed work unit than a fixed operator order | Carry the receipt-bound A348-only selection rule across multiple fresh validation fields without consuming their test feedback | Repeated selection of compact high-fidelity mechanisms without deployment feedback | Improve proposal utility with empirical family uncertainty rather than more retries |
| `H-HOLO-004` | `PARKED` | 5 | Structured polyphase or multi-slot binding can exceed equal-cell single-state holography at the same declared budget | Compare mechanistically distinct structured binding against current 83.5938% baseline | Repeated exact-rate or material accuracy gain under matched precision | Do not revisit plain equal-cell superposition; use fixed slots or learned addressing |
| `H-SHAPE-005` | `SUPPORTED` | 1 | Joint temporal profiles and XOR-neighbor/Laplacian geometry transfer more reliably than a validation-selected raw end-horizon channel | Reproduce the independently specified 532-feature transform and frozen A342 pair, then audit it without refitting | Exact independent A348/A349 score and order commitments | The transform is now an input primitive; test bounded retention rather than resweeping readers |
| `H-FRESH-006` | `PARKED` | 4 | The exact compact O1C-0007 decoder preserves useful ordering on completely untouched paired-assumption solver trajectories and improves search under equal work | Superseded as the primary next test by the full-256 Living Inverse; the frozen decoder remains available as a matched unary arm | Only useful if it lowers NLL on a full-256 public-output target | Do not generate a narrow W46 target merely to complete this historical plan |
| `H-COMPACT-007` | `ACTIVE` | 1 | Upstream bit/carry/solver events contain compressible residual structure that a bounded non-dictionary O1 state can accumulate even though their global difficulty field lacks key orientation | O1C-0016 validates 67,584-byte lifecycle but closes its fixed readout | Cross-fitted nuisance-rejected NLL lift with exact state/work accounting | Preserve bounded state; move to provenance-localized query cones if residual efficacy fails |
| `H-PAIR-008` | `ACTIVE` | 2 | Paired coordinate interventions expose reusable causal innovations after common-mode removal even though their raw global proof difficulty is non-orienting | O1C-0022 first tests the frozen incremental packet reader; the predeclared fallback preserves projected clause/proof identity and compares pair/antecedent/contradiction structure on consumed BUILD folds | Cross-key residual NLL against both common-mode and coordinate-destroying controls | Localize by assumption-rooted proof/carry ancestry and sparse Möbius pairs rather than revealed coordinate masks or another additive single-position clause reader |

## Leakage kill conditions

`H-RESIDUAL-035` controls only the native A325/A526 branch. The handoff is ready,
but those engines are not authorized until the true key can mathematically exist
in their residual domain. `H-RELATIONAL-037` may instead reduce a joint domain
and use another exact verifier; it must report real entropy/rank/search reduction,
not merely satisfaction of equations already equivalent to the public relation.
Older `ACTIVE` infrastructure, composer and packet-chain rows remain background
assets; they do not turn a wrong fixed complement into recovery evidence.

Any feature is excluded from discovery if it depends on the recovered target key,
post-result target rank, target-only internal state, outcome-conditioned file
selection or information unavailable under the billed attacker computation.
