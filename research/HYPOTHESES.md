# Hypothesis Registry

Hypotheses receive stable IDs. Status values are `OPEN`, `ACTIVE`, `SUPPORTED`,
`REFUTED`, or `PARKED`.

| ID | Status | Priority | Hypothesis | Cheapest discriminating test | Success signal | Failure pivot |
|---|---|---:|---|---|---|---|
| `H-STAGE3-001` | `ACTIVE` | 1 | Manifest-pinned pre-result solver or trajectory fields contain attacker-computable information that transfers to a held-out target ordering | Build one small frozen dataset and compare factual labels with shuffled/matched controls on disjoint source artifacts | Stable validation lift with identical work and no target-derived inputs | Localize by feature family, then pivot from aggregate metrics to temporal deltas |
| `H-MEM-002` | `SUPPORTED` | 2 | A stream-length-bounded spectral/slot state can retain the useful order of a fixed 4,096-cell Direct12 stream without explicit candidate rows | Compare global sparse Walsh, multi-slot Walsh and quantized Bit-Vault arms under exact state/work accounting | High transfer fidelity with exact state accounting | Achieved mechanistically, but O1C-0006 proves the full-rank bank is table-equivalent and larger than the matched direct table; it is a ceiling, not compression |
| `H-O1O-003` | `ACTIVE` | 2 | Adaptive operator selection gives more validation information per billed work unit than a fixed operator order | Carry the receipt-bound A348-only selection rule across multiple fresh validation fields without consuming their test feedback | Repeated selection of compact high-fidelity mechanisms without deployment feedback | Improve proposal utility with empirical family uncertainty rather than more retries |
| `H-HOLO-004` | `PARKED` | 5 | Structured polyphase or multi-slot binding can exceed equal-cell single-state holography at the same declared budget | Compare mechanistically distinct structured binding against current 83.5938% baseline | Repeated exact-rate or material accuracy gain under matched precision | Do not revisit plain equal-cell superposition; use fixed slots or learned addressing |
| `H-SHAPE-005` | `SUPPORTED` | 1 | Joint temporal profiles and XOR-neighbor/Laplacian geometry transfer more reliably than a validation-selected raw end-horizon channel | Reproduce the independently specified 532-feature transform and frozen A342 pair, then audit it without refitting | Exact independent A348/A349 score and order commitments | The transform is now an input primitive; test bounded retention rather than resweeping readers |
| `H-FRESH-006` | `PARKED` | 1 | A compact frozen O1 mechanism preserves useful ordering on a completely untouched full-round Direct12 field and improves exact recovery under equal work | First qualify a mechanism below the direct-table state baseline; only then precommit a new field and recovery budget | Better exact-recovery success/time-to-hit than numeric, public-hash and direct-table equal-work controls | Do not spend the fresh target on the ineligible full-basis ceiling |
| `H-COMPACT-007` | `ACTIVE` | 1 | Upstream bit/carry/solver evidence contains compressible structure that a sub-3,918-byte bounded O1 state can accumulate even though the final full-round scalar score field is spectrally dense | Capsule the corrected compact-support negative ladder, then compare bit/interaction accumulators on disjoint fields under a direct-table hard gate | Non-dictionary state below 3,918 bytes with stable transfer and material fidelity or time-to-hit advantage | If bit evidence also collapses, localize by round/carry/conflict provenance and retain the failure as a boundary on attacker-visible evidence |

## Leakage kill conditions

Any feature is excluded from discovery if it depends on the recovered target key,
post-result target rank, target-only internal state, outcome-conditioned file
selection or information unavailable under the billed attacker computation.
