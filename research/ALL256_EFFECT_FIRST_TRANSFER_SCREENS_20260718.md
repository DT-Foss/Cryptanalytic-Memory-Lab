# All-256 effect-first transfer screens — 2026-07-18

Recorded at `2026-07-18T19:50:13+02:00`. These were cheap retrospective screens
on already-consumed standard 20-round ChaCha20 targets. The sibling repositories
were read-only. No screen recovered a bit or made A325/W46 or A526/W52 eligible.

| Screen | Direct result | Decision |
|---|---|---|
| Frozen proof-ancestry signed-pair proxy | Pair-only BUILD-LOO mean compression `-0.094627` bit; adding the frozen O1C-0022 offset gives `-1.672151`, with the pair contribution still `-0.094627` | Close this exact second-order proxy |
| Deeper paired single-bit assumptions | The known-wrong branch of BUILD-0000 bit 0 remained `UNKNOWN` through `262,144` conflicts; that last branch cost `42.58` s | Do not scale this terminal-UNSAT shortcut to 256 bits |
| Full-candidate assumption cores | Whole-output core size `255/256`; sixteen isolated mismatching output bits produced cores of `255..256/256` | No error-bit localization; close |
| Inverse-round fixed-point update | Across 28 existing BUILD candidates, wrong-bit ranking AUC was `0.498103` from public lanes and `0.508306` from the full fixed point; Picard updates lost `115` bits in aggregate | Close raw fixed-point iteration/ranking |
| Frozen inverse top-32 correction | On two separate DEVELOPMENT targets, correct bits changed `127 -> 119` and `118 -> 112`, for `-14` total | BUILD breadcrumb was selection noise; close |
| Full-candidate one-bit contradiction neighbors | BUILD difficulty AUC was `0.473768`; the frozen reverse bottom-32 rule changed DEVELOPMENT by `-4/+6` bits, only `+2` total with opposite target behavior | Not repeated; close both orientations/top-32 |
| W8 Minesweeper cells | Correlation between solver difficulty and the number of wrong O1 bits freed was `-0.158165` on BUILD-0000 and collapsed to `-0.014003` on the unchanged BUILD-0001 repeat | No stable group evidence; do not enlarge or decode this W8 field |

The exact reuse boundary is unchanged: the sibling recovery code is a terminal
residual engine, not the missing all-256 evidence generator. The next paid work
must create an attacker-valid error-bit or completion-beam effect before adding
another state, scheduler, decoder, verifier, or residual execution.
